% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smr.R
\name{kk_smr}
\alias{kk_smr}
\title{Standardized Mortality/Morbidity Ratio (KK)}
\usage{
kk_smr(data, observed, pop, ref_rate, conf.level = 0.95, multiplier = 1000)
}
\arguments{
\item{data}{Data frame with one row per stratum (e.g. age band).}

\item{observed}{Column of observed events (bare name or string). May be a
single total; if per-stratum, values are summed.}

\item{pop}{Column of study-population person-time / counts per stratum
(bare name or string).}

\item{ref_rate}{Column of reference (standard) stratum-specific rates
(bare name or string), on the same scale as \code{1 / pop} (i.e. events per
person). Expected = pop * ref_rate.}

\item{conf.level}{Confidence level (default 0.95).}

\item{multiplier}{Multiplier for the standardized rate (default 1000).}
}
\value{
One-row tibble with observed, expected, the SMR and its exact CI, and
the indirectly standardized rate.
}
\description{
Performs indirect standardization: compares the observed number
of events in a study population with the number expected if it experienced
the age-specific rates of a reference (standard) population, yielding the
standardized mortality/morbidity ratio (SMR) with an exact Poisson
confidence interval. Complements the direct standardization in
\code{kk_std_rates}.
}
\examples{
df <- data.frame(
  age_group = c("0-39", "40-59", "60+"),
  deaths = c(5, 20, 60),
  pyears = c(20000, 15000, 8000),
  ref_rate = c(0.0002, 0.0015, 0.008)
)
kk_smr(df, deaths, pyears, ref_rate)

}
