% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_size_epi.R
\name{kk_sample_size_epi}
\alias{kk_sample_size_epi}
\title{Epidemiological Sample Size & Power Calculations}
\usage{
kk_sample_size_epi(
  design = c("cohort", "case_control_unmatched", "case_control_matched", "cluster_trial"),
  p0,
  rr_or,
  power = 0.8,
  alpha = 0.05,
  ratio = 1,
  m = 1,
  icc = 0
)
}
\arguments{
\item{design}{Study design: \code{"cohort"}, \code{"case_control_unmatched"}, \code{"case_control_matched"}, or \code{"cluster_trial"}.}

\item{p0}{Baseline probability of disease in unexposed / controls (between 0 and 1).}

\item{rr_or}{Target Relative Risk ($RR$) for cohort/trial or Odds Ratio ($OR$) for case-control studies.}

\item{power}{Target statistical power (default \code{0.80}).}

\item{alpha}{Type I error rate (default \code{0.05}).}

\item{ratio}{Ratio of unexposed to exposed subjects or controls per case (default \code{1}).}

\item{m}{Matching ratio (controls per case for matched designs) or cluster size (for cluster trials). Default \code{1}.}

\item{icc}{Intra-cluster correlation coefficient ($\rho$) for cluster-randomized trials (default \code{0}).}
}
\value{
A tidy tibble of class \code{kk_sample_size_epi} containing:
\item{design}{Study design specified}
\item{p0}{Baseline risk in unexposed / control group}
\item{p1}{Implied risk in exposed / case group}
\item{target_effect}{Target RR or OR}
\item{alpha}{Type I error rate}
\item{power}{Target statistical power}
\item{n_group1}{Required sample size for Group 1 (Exposed / Cases)}
\item{n_group2}{Required sample size for Group 2 (Unexposed / Controls)}
\item{n_total}{Total individual sample size}
\item{n_clusters}{Required number of clusters (if cluster trial)}
\item{interpretation}{Human-readable summary of sample size requirements}
}
\description{
Computes sample size and power for epidemiological study designs including cohort
studies, unmatched case-control studies, 1:m matched case-control studies, and
cluster-randomized trials with design effect adjustments.
}
\details{
Implements formulas from Woodward (2014) \emph{Epidemiology: Study Design and Data Analysis}, 3rd Edition, Chapter 8,
and Fleiss et al. (2003). For cluster-randomized trials, individual sample size is adjusted by the design effect
$DEFF = 1 + (m - 1) \times \text{icc}$.
}
\examples{
# Cohort study sample size calculation
kk_sample_size_epi(design = "cohort", p0 = 0.15, rr_or = 1.5, power = 0.80)

# 1:2 Matched Case-Control Study
kk_sample_size_epi(design = "case_control_matched", p0 = 0.20, rr_or = 2.0, ratio = 2, m = 2)

# Cluster RCT sample size
kk_sample_size_epi(design = "cluster_trial", p0 = 0.10, rr_or = 0.60, m = 25, icc = 0.03)
}
