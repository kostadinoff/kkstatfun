% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_curve.R
\name{kk_decision_curve}
\alias{kk_decision_curve}
\title{Decision Curve Analysis (KK)}
\usage{
kk_decision_curve(
  data,
  truth,
  predictor,
  thresholds = seq(0.01, 0.99, by = 0.01),
  harm = 0
)
}
\arguments{
\item{data}{Data frame.}

\item{truth}{Binary outcome column (bare name or string); coerced to 0/1.}

\item{predictor}{Predicted-probability column (bare name or string), on the
0-1 scale.}

\item{thresholds}{Numeric vector of threshold probabilities to evaluate
(default \code{seq(0.01, 0.99, by = 0.01)}).}

\item{harm}{Optional constant harm of the model/test (e.g. the cost of
obtaining the marker), on the net-benefit scale (default 0).}
}
\value{
Long tibble with one row per threshold x strategy: the \code{threshold}, the
\code{strategy} ("Model", "Treat all", "Treat none"), the \code{net_benefit}, and the
standardized net benefit (\code{net_benefit / prevalence}). Ready to plot as a
decision curve. The event prevalence is attached as attribute \code{prevalence}.
}
\description{
Evaluates the clinical utility of a risk model or diagnostic test
across a range of threshold probabilities, complementing discrimination
(\code{kk_roc}) and calibration (\code{kk_calibration}). For each threshold \code{p_t} the
net benefit weighs true positives against false positives at the odds implied
by the threshold:
\code{NB = TP/N - (FP/N) * (p_t / (1 - p_t)) - harm}.
The model is compared against the two default strategies of treating everyone
and treating no one; the model is worth using over the range of thresholds
where its net benefit is highest.
}
\examples{
\dontrun{
df <- data.frame(disease = rbinom(500, 1, 0.3))
df$risk <- plogis(qlogis(0.3) + 0.9 * df$disease + rnorm(500))
kk_decision_curve(df, disease, risk)
}

}
