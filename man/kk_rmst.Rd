% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmst.R
\name{kk_rmst}
\alias{kk_rmst}
\title{Restricted Mean Survival Time (KK)}
\usage{
kk_rmst(data, time, status, group = NULL, tau = NULL, conf.level = 0.95)
}
\arguments{
\item{data}{Data frame.}

\item{time}{Follow-up time column (bare name or string).}

\item{status}{Event indicator column (bare name or string); 0/1.}

\item{group}{Optional two-level grouping column (bare name or string). If
omitted, a single overall RMST is returned.}

\item{tau}{Restriction horizon. If NULL (default), the largest time at which
all groups still have subjects at risk (the minimum of the group-specific
maximum follow-up times) is used.}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
Tibble with the RMST and CI for each group and, when two groups are
supplied, a row for the RMST difference and ratio with CIs and p-values.
}
\description{
Computes the restricted mean survival time (RMST) up to a
horizon \code{tau} for each group and the between-group difference and ratio.
RMST is the area under the Kaplan-Meier curve up to \code{tau} (mean event-free
time within the first \code{tau} units) and is a useful, assumption-light
alternative to the hazard ratio when the proportional-hazards assumption
is in doubt.
}
\examples{
\dontrun{
library(survival)
# Time-to-death by sex, restricted to 1 year
kk_rmst(lung, time, status, sex, tau = 365)
}

}
