% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sero_incidence.R
\name{kk_sero_incidence}
\alias{kk_sero_incidence}
\title{Incidence / Force of Infection from Seroprevalence Surveys (KK)}
\usage{
kk_sero_incidence(
  data,
  age,
  positive,
  year,
  total = NULL,
  method = c("cohort", "catalytic"),
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame. Either aggregated (one row per age x survey with
\code{positive} and \code{total} counts) or individual-level (one row per person with a
0/1 \code{positive}, aggregated internally when \code{total} is omitted).}

\item{age}{Age or age-group midpoint column (bare name or string); numeric.}

\item{positive}{Seropositive count (aggregated) or 0/1 indicator (individual).}

\item{year}{Calendar year of the survey (bare name or string); numeric.}

\item{total}{Sample size per age x survey (aggregated input). If omitted,
\code{positive} is treated as an individual 0/1 indicator and counts are formed by
aggregation.}

\item{method}{\code{"cohort"} (default, between-survey) or \code{"catalytic"}
(within-survey age gradient).}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
Tibble of FOI (annual incidence rate among susceptibles) estimates with
confidence intervals. For \code{"cohort"}: one row per cohort per between-survey
interval (with ages, years and seroprevalences at each end). For
\code{"catalytic"}: one row per survey per age interval.
}
\description{
Estimates the incidence rate (force of infection, FOI) of an
infection from repeated cross-sectional seroprevalence surveys, using the
catalytic (irreversible-seroconversion) model in which the seronegative
("susceptible") fraction is \code{S = 1 - seroprevalence} and
\verb{S(a) = exp(-\\int_0^a lambda)}.

Two methods are provided:
\describe{
\item{\code{"cohort"} (default)}{Follows each \strong{birth cohort} (\code{cohort = year - age}) across two or more surveys and estimates the FOI it experienced
between consecutive surveys as \verb{lambda = -[log S2 - log S1] / (t2 - t1)}.
This is a genuine longitudinal incidence for that cohort over the ages
and calendar years between surveys, so it captures age and cohort effects
directly. Requires >= 2 surveys, spaced so that cohorts realign across
age groups.}
\item{\code{"catalytic"}}{Within each survey, estimates the age-specific FOI from
the rise in seroprevalence between consecutive age groups,
\verb{lambda(a) = -[log S(a2) - log S(a1)] / (a2 - a1)} (a cross-sectional
estimate that assumes stationarity).}
}
}
\examples{
# Two surveys 10 years apart; true constant FOI = 0.05 / year
set.seed(1)
make_survey <- function(year, foi = 0.05, ages = seq(5, 65, 10), n = 500) {
  p <- 1 - exp(-foi * ages)
  data.frame(age = ages, year = year,
             positive = rbinom(length(ages), n, p), total = n)
}
sero <- rbind(make_survey(2000), make_survey(2010))
kk_sero_incidence(sero, age, positive, year, total = total)

}
