% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediation.R
\name{kk_causal_mediation}
\alias{kk_causal_mediation}
\title{Causal Mediation Analysis (KK)}
\usage{
kk_causal_mediation(
  data,
  exposure,
  mediator,
  outcome,
  confounders = NULL,
  mediator_family = "gaussian",
  outcome_family = "binomial",
  boot_reps = 1000,
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame.}

\item{exposure}{Exposure column (bare name or string).}

\item{mediator}{Continuous mediator column (bare name or string).}

\item{outcome}{Outcome column (bare name or string).}

\item{confounders}{Optional character vector of confounder columns entered in
both models.}

\item{mediator_family}{Family for the (linear) mediator model, default
\code{"gaussian"}.}

\item{outcome_family}{Family for the outcome model: \code{"binomial"} (default,
odds-ratio scale) or \code{"gaussian"} (mean-difference scale).}

\item{boot_reps}{Number of bootstrap replications for confidence intervals
(default 1000).}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
Tibble with the Total, Direct (NDE), Indirect (NIE) effects and the
Proportion Mediated, each with a bootstrap confidence interval, plus the
scale of the effect.
}
\description{
Decomposes the total effect of an exposure on an outcome into a
natural direct effect (NDE, not operating through the mediator) and a natural
indirect effect (NIE, operating through the mediator), using the
regression-based product method (Baron-Kenny / VanderWeele). A mediator model
and an outcome model are fitted; effects are formed from their coefficients
and confidence intervals are obtained by nonparametric bootstrap.

This implementation assumes a \strong{continuous mediator} and \strong{no
exposure-mediator interaction}. For a binary outcome the effects are
reported on the odds-ratio scale under the rare-outcome approximation; for a
continuous outcome they are mean differences. Valid causal interpretation
further requires no unmeasured exposure-outcome, mediator-outcome, or
exposure-mediator confounding (adjust for these via \code{confounders}).
}
\examples{
\dontrun{
set.seed(1)
n <- 500
x <- rbinom(n, 1, 0.5)
m <- 0.5 * x + rnorm(n)                       # exposure -> mediator
y <- rbinom(n, 1, plogis(-1 + 0.3 * x + 0.6 * m))  # both paths
df <- data.frame(x = x, m = m, y = y)
kk_causal_mediation(df, x, m, y, boot_reps = 500)
}

}
