% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reclassification.R
\name{kk_calibration}
\alias{kk_calibration}
\title{Model Calibration & Goodness-of-Fit}
\usage{
kk_calibration(
  data,
  outcome,
  pred_prob,
  g = 10,
  method = c("hosmer_lemeshow", "greenwood_nam"),
  alpha = 0.05
)
}
\arguments{
\item{data}{A data frame or tibble containing patient-level data.}

\item{outcome}{Column name (quoted or unquoted) for binary patient outcome (1/0 or TRUE/FALSE).}

\item{pred_prob}{Column name (quoted or unquoted) for predicted probabilities.}

\item{g}{Number of risk groups/quantiles for calibration evaluation (default \code{10}).}

\item{method}{Calibration test method (\code{"hosmer_lemeshow"} or \code{"greenwood_nam"}).}

\item{alpha}{Significance level for confidence intervals (default \code{0.05}).}
}
\value{
A tidy tibble of class \code{kk_calibration} containing calibration statistics and slope/intercept estimates.
}
\description{
Assesses risk prediction model calibration on patient-level data using the Hosmer-Lemeshow
test, Observed-to-Expected (O/E) ratio, calibration slope, and calibration intercept.
}
\examples{
library(dplyr)
set.seed(123)
calib_df <- tibble(
  outcome = rbinom(300, 1, 0.20),
  pred_prob = runif(300, 0.05, 0.40)
)
kk_calibration(calib_df, outcome, pred_prob, g = 10)
}
\seealso{
\code{\link[=kk_calibration_table]{kk_calibration_table()}} for the per-group calibration table (with
Brier score) used to draw a calibration curve.
}
