% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infectious_models.R
\name{kk_seir}
\alias{kk_seir}
\title{Deterministic SIR / SEIR Transmission Model (KK)}
\usage{
kk_seir(
  beta,
  gamma,
  sigma = NULL,
  S0,
  I0,
  R0_init = 0,
  E0 = 0,
  times = 0:180,
  mu = 0,
  nu = 0
)
}
\arguments{
\item{beta}{Transmission rate (effective contact rate).}

\item{gamma}{Recovery rate; the mean infectious period is \code{1 / gamma}.}

\item{sigma}{Rate of progression from exposed to infectious (\code{1 / sigma} is the
mean latent period). \code{NULL} (default) gives an SIR model with no latent
stage.}

\item{S0, I0, R0_init}{Initial number susceptible, infectious, and recovered.
\code{E0} initialises the exposed compartment for SEIR models.}

\item{E0}{Initial number exposed (SEIR only, default 0).}

\item{times}{Numeric vector of time points to report (default \code{0:180}).}

\item{mu}{Per-capita birth = death rate for vital dynamics (default 0, a
closed population).}

\item{nu}{Per-capita vaccination rate moving susceptibles directly to
recovered/immune (default 0).}
}
\value{
Tibble of the compartment sizes at each time point, plus \code{incidence}
(new infections per step) and the cumulative incidence \code{C}. The attribute
\code{R0} holds the basic reproduction number implied by the parameters.
}
\description{
Simulates a deterministic compartmental epidemic model. With
\code{sigma = NULL} an SIR model is solved; supplying a finite \code{sigma} adds an
exposed (latent) compartment for an SEIR model. Optional vital dynamics
(\code{mu}) and a constant vaccination rate (\code{nu}) are supported. The system is
integrated with a built-in Runge-Kutta solver, so no external ODE package is
required. The basic reproduction number \code{R0} implied by the parameters is
returned as an attribute.
}
\examples{
# Classic closed SIR outbreak, R0 = 2.5
sir <- kk_seir(beta = 0.5, gamma = 0.2, S0 = 999, I0 = 1, R0_init = 0)
attr(sir, "R0")

# SEIR with a 5-day latent period
seir <- kk_seir(beta = 0.6, gamma = 0.2, sigma = 0.2,
                S0 = 9999, I0 = 1, R0_init = 0, times = 0:120)

}
