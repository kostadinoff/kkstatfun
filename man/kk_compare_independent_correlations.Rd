% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation_tests.R
\name{kk_compare_independent_correlations}
\alias{kk_compare_independent_correlations}
\title{Compare Independent Correlations}
\usage{
kk_compare_independent_correlations(
  data,
  x = NULL,
  y = NULL,
  group = NULL,
  r = NULL,
  n = NULL,
  alternative = c("two.sided", "less", "greater"),
  conf.level = 0.95
)
}
\arguments{
\item{data}{A data frame or a vector. If a data frame is provided, can specify either raw variables
(\code{x}, \code{y}, \code{group}) or pre-calculated fields (\code{r}, \code{n}).}

\item{x}{If \code{data} is a data frame and raw data is used, the column representing the first continuous variable.
If \code{data} is a vector, the sample sizes (\code{n}) must be provided here.}

\item{y}{If \code{data} is a data frame and raw data is used, the column representing the second continuous variable.}

\item{group}{If \code{data} is a data frame and raw data is used, the column defining the independent groups.}

\item{r}{If \code{data} is a data frame and pre-calculated statistics are used, the column name containing correlations.}

\item{n}{If \code{data} is a data frame and pre-calculated statistics are used, the column name containing sample sizes.}

\item{alternative}{Character string specifying the alternative hypothesis for $k = 2$ tests.
Must be one of \code{"two.sided"} (default), \code{"less"}, or \code{"greater"}.}

\item{conf.level}{Confidence level for the interval (default: 0.95).}
}
\value{
A tibble with the following columns:
\itemize{
\item \code{method}: The test method used.
\item \code{alternative}: The alternative hypothesis.
\item \code{statistic_name}: Name of the test statistic ("Z" or "Chi-Square").
\item \code{statistic}: The test statistic value.
\item \code{df}: The degrees of freedom for the homogeneity test (or \code{NA} for $k = 2$).
\item \code{p_value}: The p-value of the test.
\item \code{common_r}: The weighted estimate of the common population correlation.
\item \code{group_summaries}: A summary string of the correlation and sample size for each group.
}
}
\description{
Evaluates whether two or $k$ independent correlation coefficients differ significantly
using Fisher's z-transformation. If $k = 2$, it performs a standard normal Z-test. If $k > 2$,
it performs a Chi-Square test of homogeneity.
}
\examples{
# Example 28c: Comparing two independent correlations
r_vals <- c(0.955, 0.765)
n_vals <- c(5, 5)
kk_compare_independent_correlations(r_vals, n_vals)

# Example 28d: Homogeneity of three independent correlations
r_three <- c(0.955, 0.765, 0.845)
n_three <- c(5, 5, 5)
kk_compare_independent_correlations(r_three, n_three)

}
