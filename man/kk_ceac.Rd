% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health_economics.R
\name{kk_ceac}
\alias{kk_ceac}
\title{Cost-Effectiveness Acceptability Curve (KK)}
\usage{
kk_ceac(data, sim, strategy, cost, effect, wtp = seq(0, 1e+05, by = 5000))
}
\arguments{
\item{data}{Long-format data frame of PSA draws with one row per
simulation x strategy.}

\item{sim}{Column identifying the simulation / iteration (bare name or string).}

\item{strategy}{Column of strategy labels (bare name or string).}

\item{cost}{Column of simulated costs (bare name or string).}

\item{effect}{Column of simulated effects (bare name or string).}

\item{wtp}{Numeric vector of willingness-to-pay thresholds to evaluate.}
}
\value{
Tibble (strategies x thresholds) with \code{prob_ce} (probability the
strategy is cost-effective, i.e. highest NMB) and \code{on_frontier} (whether the
strategy has the highest mean NMB at that threshold).
}
\description{
Builds a cost-effectiveness acceptability curve (CEAC) from the
output of a probabilistic sensitivity analysis (PSA). For each
willingness-to-pay threshold the function computes, across simulations, the
probability that each strategy yields the highest net monetary benefit. The
cost-effectiveness acceptability frontier (the strategy optimal on the
expected-NMB frontier) is also identified.
}
\examples{
set.seed(1)
psa <- do.call(rbind, lapply(1:200, function(i) {
  data.frame(
    sim = i,
    strategy = c("A", "B"),
    cost = c(rnorm(1, 8000, 1500), rnorm(1, 12000, 2000)),
    effect = c(rnorm(1, 5.0, 0.3), rnorm(1, 5.5, 0.3))
  )
}))
kk_ceac(psa, sim, strategy, cost, effect, wtp = seq(0, 60000, 10000))

}
