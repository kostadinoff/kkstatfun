% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette.R
\name{set_plot_colors}
\alias{set_plot_colors}
\title{Set the Default Discrete Plot Palette}
\usage{
set_plot_colors(colors, n_max = 24, set_default = TRUE)
}
\arguments{
\item{colors}{Character vector of anchor colours (hex codes), e.g. three flag
colours.}

\item{n_max}{Largest number of discrete levels to pre-build palettes for
(default 24).}

\item{set_default}{Whether to register the palette as the global ggplot2
default for discrete colour and fill (default TRUE).}
}
\value{
Invisibly, the validated anchor colours.
}
\description{
Registers a set of anchor ("flag") colours and makes every
discrete \code{ggplot2} colour and fill scale draw from them by default — so
\code{\link[=kkplot]{kkplot()}} (and any \code{ggplot()}) automatically uses your chosen colours for
grouped bars, points, lines, etc. Continuous colour/fill scales (e.g.
heatmaps) are left untouched. Fewer groups than anchors use the anchor
colours directly; more groups interpolate between them.

This is the colour counterpart to \code{\link[=set_plot_font]{set_plot_font()}}; a typical analysis
preamble is \verb{kk_setup(); set_plot_font("Roboto Condensed", size = 14); set_plot_colors(c("#D62828", "#003049", "#F77F00"))}.
}
\examples{
\dontrun{
set_plot_colors(c("#D62828", "#003049", "#F77F00"))
# now discrete fills/colours use the flag palette automatically
kkplot(mtcars, ggplot2::aes(factor(cyl), fill = factor(cyl))) +
  ggplot2::geom_bar()
}

}
