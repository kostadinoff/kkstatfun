% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival_models.R
\name{kk_coxph}
\alias{kk_coxph}
\title{Cox Proportional Hazards Model (KK)}
\usage{
kk_coxph(data, time, status, predictors, conf.level = 0.95, ...)
}
\arguments{
\item{data}{Data frame.}

\item{time}{Follow-up time column (bare name or string).}

\item{status}{Event indicator column (bare name or string); 0/1, 1/2, logical,
or a \code{Surv}-compatible coding.}

\item{predictors}{Character vector of predictor column names.}

\item{conf.level}{Confidence level (default 0.95).}

\item{...}{Additional arguments passed to \code{survival::coxph}.}
}
\value{
Tibble with one row per model term containing the hazard ratio,
confidence interval, p-value, model type (univariate / multivariable),
the per-term Schoenfeld p-value (\code{ph_p}), and the global PH-test p-value
(\code{ph_global_p}).
}
\description{
Fits univariate and multivariable Cox proportional hazards models
and returns a tidy hazard-ratio table together with the Schoenfeld residual
test of the proportional-hazards assumption (\code{survival::cox.zph}).
}
\examples{
\dontrun{
library(survival)
kk_coxph(lung, time, status, predictors = c("age", "sex", "ph.ecog"))
}

}
