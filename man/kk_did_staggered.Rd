% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did.R
\name{kk_did_staggered}
\alias{kk_did_staggered}
\title{Group-Time Average Treatment Effects with Staggered Adoption (KK)}
\usage{
kk_did_staggered(
  data,
  outcome,
  unit,
  time,
  treat_time,
  covariates = NULL,
  control = c("notyettreated", "nevertreated"),
  base_period = c("universal", "varying"),
  n_boot = 200,
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame in long panel form, one row per unit-period.}

\item{outcome}{Outcome column (bare name or string).}

\item{unit}{Panel identifier column (bare name or string).}

\item{time}{Period column (bare name or string); numeric.}

\item{treat_time}{Adoption-period column (bare name or string); \code{NA} or
\code{Inf} marks never-treated units.}

\item{covariates}{Optional character vector of time-invariant covariate
names. When supplied, each ATT(g, t) is estimated by outcome regression:
the change in outcome is modelled among the comparison units and the
fitted values are subtracted from the treated units' changes.}

\item{control}{Comparison group: \code{"notyettreated"} (default, uses both
never-treated and not-yet-treated units) or \code{"nevertreated"}.}

\item{base_period}{\code{"universal"} (default) compares every period with the
period before adoption; \code{"varying"} compares each pre-period with the one
immediately before it, which is more sensitive to short-run pre-trends.}

\item{n_boot}{Bootstrap replicates (default 200). Raise to 999+ for
published estimates.}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
A named list of tibbles:
\describe{
\item{att_gt}{One row per cohort-period cell: \code{group}, \code{time},
\code{rel_time}, \code{estimate}, \code{std.error}, pointwise and simultaneous
confidence bounds, \code{p.value} and cell sizes.}
\item{dynamic}{Event study averaged by time since adoption.}
\item{group}{One post-treatment average per adoption cohort.}
\item{calendar}{One average per calendar period.}
\item{overall}{One-row summary: the cohort-size-weighted average of all
post-treatment ATT(g, t).}
}
}
\description{
Difference-in-differences when units adopt the treatment at
different times. Instead of one two-way fixed-effects coefficient - which
under staggered adoption uses already-treated units as controls and can
carry the wrong sign when effects change over time - this estimates a
separate 2x2 comparison ATT(g, t) for every adoption cohort \code{g} and every
period \code{t}, always against units not yet treated, and then aggregates.

Four aggregations are returned: \code{overall} (one number, the average effect
across all post-treatment cell), \code{dynamic} (an event study by time since
adoption), \code{group} (one effect per adoption cohort) and \code{calendar} (one
effect per period). Standard errors come from a unit-level nonparametric
bootstrap, which respects the within-unit dependence of a panel;
simultaneous bands use the bootstrapped maximum t-statistic, so the whole
set of intervals covers at \code{conf.level} jointly rather than one at a time.
}
\examples{
set.seed(3)
units <- 90
d <- expand.grid(unit = seq_len(units), time = 1:6)
adopt <- rep(c(3, 5, Inf), each = units / 3)
d$treat_time <- adopt[d$unit]
d$y <- 4 + 0.2 * d$time + rnorm(nrow(d), 0, 0.4) +
  1.5 * pmax(0, d$time - d$treat_time + 1)

res <- kk_did_staggered(d, y, unit, time, treat_time, n_boot = 50)
res$overall
res$dynamic

}
\references{
Callaway B, Sant'Anna PHC (2021) Difference-in-differences with
multiple time periods. \emph{Journal of Econometrics} 225:200-230.
}
\seealso{
\code{\link[=kk_did]{kk_did()}}, \code{\link[=kk_event_study]{kk_event_study()}}
}
