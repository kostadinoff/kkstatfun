% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color_science.R
\name{kk_cvd}
\alias{kk_cvd}
\title{Simulate Colour Vision Deficiency}
\usage{
kk_cvd(colors, type = c("deutan", "protan", "tritan", "achroma"), severity = 1)
}
\arguments{
\item{colors}{Character vector of colours (hex codes or R colour names).}

\item{type}{Vision types to simulate: any of \code{"deutan"}, \code{"protan"},
\code{"tritan"}, \code{"achroma"} and \code{"normal"} (default: the first four).}

\item{severity}{Severity of the deficiency, 0 (normal vision) to 1 (full
dichromacy, the default). Intermediate values are interpolated within the
published table and correspond to \emph{anomalous trichromacy}.}
}
\value{
A tibble with one row per colour x type: \code{color}, \code{hex}, \code{vision},
\code{severity}, \code{simulated} (hex as seen).
}
\description{
Renders colours as they are seen with the common forms of
colour blindness, using the physiologically-derived transforms of Machado,
Oliveira & Fernandes (2009) applied in linear-light RGB. Roughly 8\% of men
and 0.5\% of women have a red-green deficiency, so a figure whose groups are
separated by colour alone should be checked before submission.
}
\examples{
kk_cvd(c("#D62828", "#003049", "#F77F00"))
kk_cvd("#D62828", type = "deutan", severity = 0.5)

}
\references{
Machado, G. M., Oliveira, M. M. & Fernandes, L. A. F. (2009).
A physiologically-based model for simulation of color vision deficiency.
\emph{IEEE Transactions on Visualization and Computer Graphics}, 15(6),
1291-1298.
}
\seealso{
\code{\link[=kk_show_cvd]{kk_show_cvd()}} to preview, \code{\link[=kk_pal_check]{kk_pal_check()}} to test a whole
palette, \code{\link[=kk_pal_safe]{kk_pal_safe()}} to build one that passes.
}
