% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rdd.R
\name{kk_rkd}
\alias{kk_rkd}
\title{Regression Kink Design (KK)}
\usage{
kk_rkd(
  data,
  outcome,
  running,
  cutoff = 0,
  treatment = NULL,
  bandwidth = NULL,
  kernel = c("triangular", "uniform", "epanechnikov"),
  degree = 1,
  covariates = NULL,
  vcov_type = "HC1",
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame.}

\item{outcome}{Outcome column (bare name or string).}

\item{running}{Running variable column.}

\item{cutoff}{Kink point on the running variable (default 0).}

\item{treatment}{Optional policy/treatment variable for a fuzzy kink.}

\item{bandwidth}{Bandwidth. \code{NULL} (default) uses a rule-of-thumb
\code{1.84 * sd(x) * n^(-1/7)}, the kink-appropriate rate; supplying your own
bandwidth is strongly preferred.}

\item{kernel}{Weighting kernel: \code{"triangular"} (default), \code{"uniform"} or
\code{"epanechnikov"}.}

\item{degree}{Local polynomial order (default 1).}

\item{covariates}{Optional character vector of covariate names.}

\item{vcov_type}{Robust variance type (default \code{"HC1"}).}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
One-row tibble with the kink \code{estimate} (slope change, or the ratio
of slope changes when fuzzy), \code{std.error}, confidence interval, \code{p.value},
bandwidth and effective sample sizes. The bandwidth sensitivity table is
attached as the attribute \code{bw_sensitivity}.
}
\description{
Estimates a causal effect from a change in \emph{slope} rather than
a jump in level. Where a policy formula bends at a threshold - a benefit
that is flat up to an income limit and then tapers, a fee that starts
rising at a size cut-off - the kink in the assignment rule induces a kink
in the outcome, and the ratio of the two slope changes identifies the
effect at the threshold.

With \code{treatment} supplied the estimate is a fuzzy kink: the change in the
outcome slope divided by the change in the treatment slope. Otherwise the
returned estimate is the change in the outcome slope itself.

Kink designs are more demanding than discontinuity designs: slopes are
estimated less precisely than levels, so wider bandwidths and larger
samples are needed, and the estimate is more sensitive to the polynomial
order. Report the bandwidth sensitivity table.
}
\examples{
set.seed(5)
n <- 1500
x <- runif(n, -1, 1)
y <- 1 + 0.5 * x + 1.5 * pmax(0, x) + rnorm(n, 0, 0.25)
d <- data.frame(x = x, y = y)

kk_rkd(d, y, x, bandwidth = 0.6)

}
\references{
Card D, Lee DS, Pei Z, Weber A (2015) Inference on causal
effects in a generalized regression kink design. \emph{Econometrica}
83:2453-2483.
}
\seealso{
\code{\link[=kk_rdd]{kk_rdd()}}
}
