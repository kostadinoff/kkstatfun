% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{kk_model}
\alias{kk_model}
\title{Extract a Fitted Model from a kkstatfun Result}
\usage{
kk_model(
  x,
  which = c("multivariable", "univariate", "all"),
  predictor = NULL,
  group = NULL
)
}
\arguments{
\item{x}{A tibble returned by a kkstatfun modelling function.}

\item{which}{Which model to return: \code{"multivariable"} (default, the adjusted
model), \code{"univariate"} (requires \code{predictor}), or \code{"all"} for the full
named list.}

\item{predictor}{Predictor name, when \code{which = "univariate"}.}

\item{group}{For a result produced from grouped data, the group label whose
model you want (see \code{names(attr(x, "group_models"))}). \code{NULL} (default)
for ungrouped results.}
}
\value{
The fitted model object (\code{lm}, \code{glm}, \code{coxph}, \code{polr}, ...), or a
named list when \code{which = "all"}.
}
\description{
The modelling functions (\code{\link[=kk_reg]{kk_reg()}}, \code{\link[=kk_coxph]{kk_coxph()}}, \code{\link[=kk_rr_reg]{kk_rr_reg()}},
\code{\link[=kk_rate_reg]{kk_rate_reg()}}, \code{\link[=kk_firth]{kk_firth()}}) return a tidy tibble of coefficients, but
also keep the underlying fitted model. \code{kk_model()} retrieves it so you can
continue with the wider modelling ecosystem -- most usefully \code{emmeans} for
estimated marginal means, contrasts and trends.
}
\details{
\code{emmeans} recovers the model frame from the fitted object, so the returned
model works directly:

\if{html}{\out{<div class="sourceCode">}}\preformatted{fit <- kk_reg(d, sbp, c("age", "arm"))
emmeans::emmeans(kk_model(fit), ~ arm)
}\if{html}{\out{</div>}}

If a model is ever moved between sessions and \code{emmeans} cannot recover the
data, pass it explicitly with \code{data = kk_model_data(fit)}.
}
\examples{
set.seed(1)
d <- data.frame(
  age = rnorm(100, 60, 10),
  arm = factor(sample(c("Control", "Treatment"), 100, TRUE))
)
d$sbp <- 100 + 0.4 * d$age - 6 * (d$arm == "Treatment") + rnorm(100, 0, 8)

fit <- kk_reg(d, sbp, c("age", "arm"))
summary(kk_model(fit))
names(kk_model(fit, "all")$univariate)

}
\seealso{
\code{\link[=kk_model_data]{kk_model_data()}}, \code{\link[=kk_emmeans]{kk_emmeans()}}
}
