% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_stats.R
\name{kk_epi_stats}
\alias{kk_epi_stats}
\title{Calculate Odds Ratio and Relative Risk}
\usage{
kk_epi_stats(data = NULL, exposure, outcome, conf.level = 0.95)
}
\arguments{
\item{data}{Optional data frame. If provided, \code{exposure} and \code{outcome} should be column names.}

\item{exposure}{Exposure variable (vector or column name). Must be binary.}

\item{outcome}{Outcome variable (vector or column name). Must be binary.}

\item{conf.level}{Confidence level (default: 0.95)}
}
\value{
Tibble with OR, RR, and their confidence intervals
}
\description{
Computes Odds Ratio (OR) and Relative Risk (RR) from a 2x2 contingency table
or from two binary vectors.
}
\examples{
# Create dummy data
df <- data.frame(
              exposure = c(rep(1, 50), rep(0, 50)),
              outcome = c(rep(1, 20), rep(0, 30), rep(1, 10), rep(0, 40))
)

# Calculate stats
kk_epi_stats(df, exposure, outcome)

}
