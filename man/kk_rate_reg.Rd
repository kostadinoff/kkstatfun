% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_regression.R
\name{kk_rate_reg}
\alias{kk_rate_reg}
\alias{kk_poisson}
\title{Poisson / Negative-Binomial Rate Regression (KK)}
\usage{
kk_rate_reg(
  data,
  outcome,
  predictors,
  person_time = NULL,
  conf.level = 0.95,
  dispersion_threshold = 1.5,
  ...
)

kk_poisson(
  data,
  outcome,
  predictors,
  person_time = NULL,
  conf.level = 0.95,
  dispersion_threshold = 1.5,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{outcome}{Count outcome column (bare name or string).}

\item{predictors}{Character vector of predictor column names.}

\item{person_time}{Optional column of person-time; when supplied it enters the
model as \code{offset(log(person_time))}, turning counts into rates.}

\item{conf.level}{Confidence level (default 0.95).}

\item{dispersion_threshold}{Pearson dispersion above which a negative-binomial
model is preferred (default 1.5).}

\item{...}{Additional arguments passed to the fitting function.}
}
\value{
Tibble with one row per term: the IRR, confidence interval, p-value,
model type (univariate / multivariable), the fitted family, and the Pearson
dispersion statistic.
}
\description{
Fits univariate and multivariable Poisson regression models for
count or rate outcomes and returns incidence-rate ratios (IRR). Overdispersion
is assessed via the Pearson dispersion statistic; when it is substantial and
\code{MASS} is available, a negative-binomial model is fitted instead so the
standard errors are not understated.
}
\examples{
\dontrun{
df <- data.frame(
  cases = rpois(100, 3),
  pyears = runif(100, 50, 150),
  age = rnorm(100, 50, 10),
  sex = rbinom(100, 1, 0.5)
)
kk_rate_reg(df, cases, c("age", "sex"), person_time = pyears)
}

}
