% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc.R
\name{kk_compare_roc}
\alias{kk_compare_roc}
\title{Compare Two ROC Curves (DeLong Test) (KK)}
\usage{
kk_compare_roc(
  data,
  truth,
  predictor1,
  predictor2,
  method = "delong",
  paired = TRUE,
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame.}

\item{truth}{Binary outcome column (bare name or string).}

\item{predictor1}{First marker column (bare name or string).}

\item{predictor2}{Second marker column (bare name or string).}

\item{method}{Test method passed to \code{pROC::roc.test} (default "delong").}

\item{paired}{Whether the markers are paired on the same subjects
(default TRUE).}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
Tibble with the two AUCs, their difference, the test statistic,
and the p-value.
}
\description{
Compares the AUCs of two markers measured on the same subjects
using DeLong's test for paired ROC curves (via \code{pROC::roc.test}).
}
\examples{
\dontrun{
df <- data.frame(
  disease = rbinom(200, 1, 0.4),
  marker_a = rnorm(200),
  marker_b = rnorm(200)
)
kk_compare_roc(df, disease, marker_a, marker_b)
}

}
