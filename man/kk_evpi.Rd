% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health_economics.R
\name{kk_evpi}
\alias{kk_evpi}
\title{Expected Value of Perfect Information (KK)}
\usage{
kk_evpi(
  data,
  sim,
  strategy,
  cost,
  effect,
  wtp = seq(0, 1e+05, by = 5000),
  pop_size = NULL,
  horizon = NULL,
  disc_rate = 0.03
)
}
\arguments{
\item{data}{Long-format data frame of PSA draws with one row per
simulation x strategy.}

\item{sim}{Column identifying the simulation / iteration (bare name or string).}

\item{strategy}{Column of strategy labels (bare name or string).}

\item{cost}{Column of simulated costs (bare name or string).}

\item{effect}{Column of simulated effects (bare name or string).}

\item{wtp}{Numeric vector of willingness-to-pay thresholds to evaluate.}

\item{pop_size}{Optional size of the affected population; when supplied the
population EVPI is returned alongside the per-patient value.}

\item{horizon}{Optional decision time horizon in years over which the
population bears the decision (multiplies the population, discounted at
\code{disc_rate}). Ignored if \code{pop_size} is NULL.}

\item{disc_rate}{Annual discount rate applied over \code{horizon} (default 0.03).}
}
\value{
Tibble with one row per threshold: the per-patient \code{evpi}, the
strategy optimal on the expected-NMB frontier (\code{optimal_strategy}), and, when
\code{pop_size} is supplied, \code{population_evpi}.
}
\description{
Computes the expected value of perfect information (EVPI) from the
output of a probabilistic sensitivity analysis (PSA), quantifying the
expected cost of decision uncertainty at each willingness-to-pay threshold.
EVPI is the difference between the expected net benefit under perfect
information (choosing the best strategy in every simulation) and the expected
net benefit of the single strategy that is best on average:
\verb{EVPI = E_theta[max_d NMB(d, theta)] - max_d E_theta[NMB(d, theta)]}.
Shares the PSA input format of \code{\link[=kk_ceac]{kk_ceac()}}.
}
\examples{
set.seed(1)
psa <- do.call(rbind, lapply(1:400, function(i) {
  data.frame(
    sim = i,
    strategy = c("A", "B"),
    cost = c(rnorm(1, 8000, 1500), rnorm(1, 12000, 2000)),
    effect = c(rnorm(1, 5.0, 0.3), rnorm(1, 5.5, 0.3))
  )
}))
kk_evpi(psa, sim, strategy, cost, effect,
        wtp = seq(0, 60000, 10000), pop_size = 5000, horizon = 10)

}
