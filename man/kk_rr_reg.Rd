% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_regression.R
\name{kk_rr_reg}
\alias{kk_rr_reg}
\title{Adjusted Risk-Ratio Regression (Modified Poisson) (KK)}
\usage{
kk_rr_reg(data, outcome, predictors, conf.level = 0.95, vcov_type = "HC0")
}
\arguments{
\item{data}{Data frame.}

\item{outcome}{Binary outcome column (bare name or string); coerced to 0/1.}

\item{predictors}{Character vector of predictor column names.}

\item{conf.level}{Confidence level (default 0.95).}

\item{vcov_type}{Sandwich estimator type passed to \code{sandwich::vcovHC}
(default "HC0", as in Zou 2004).}
}
\value{
Tibble with one row per term: the adjusted risk ratio, robust
confidence interval, robust p-value, and model type
(univariate / multivariable).
}
\description{
Estimates adjusted risk ratios for a binary outcome using the
modified Poisson approach of Zou (2004): a log-link Poisson model with robust
(sandwich) standard errors. This avoids the convergence problems of
log-binomial regression while yielding valid risk ratios rather than odds
ratios, which are preferable for common outcomes in cohort studies.
}
\examples{
\dontrun{
df <- data.frame(
  event = rbinom(200, 1, 0.3),
  exposure = rbinom(200, 1, 0.5),
  age = rnorm(200, 50, 10)
)
kk_rr_reg(df, event, c("exposure", "age"))
}

}
