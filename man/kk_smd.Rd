% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balance.R
\name{kk_smd}
\alias{kk_smd}
\alias{kk_balance_table}
\title{Standardized Mean Differences for Covariate Balance (KK)}
\usage{
kk_smd(data, treatment, variables = NULL, threshold = 0.1)

kk_balance_table(data, treatment, variables = NULL, threshold = 0.1)
}
\arguments{
\item{data}{Data frame.}

\item{treatment}{Two-level grouping column (bare name or string).}

\item{variables}{Character vector of covariate columns. If NULL, all columns
except \code{treatment} are used.}

\item{threshold}{Absolute SMD above which a covariate is flagged as imbalanced
(default 0.1).}
}
\value{
Tibble with one row per covariate (or per level for categoricals):
the summary in each group, the SMD, and an \code{imbalanced} flag.
}
\description{
Computes standardized mean differences (SMD) between two groups
for each covariate, the standard diagnostic for baseline balance in matched,
weighted, or propensity-score analyses. Continuous variables use the pooled
standard deviation; binary and categorical variables use the proportion-based
SMD (one row per non-reference level). An absolute SMD above \code{threshold}
(conventionally 0.1) flags meaningful imbalance.
}
\examples{
df <- data.frame(
  arm = rep(c("treated", "control"), each = 50),
  age = c(rnorm(50, 55, 8), rnorm(50, 50, 8)),
  smoker = rbinom(100, 1, 0.4)
)
kk_smd(df, arm, variables = c("age", "smoker"))

}
