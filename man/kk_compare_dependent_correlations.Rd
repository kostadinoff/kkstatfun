% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation_tests.R
\name{kk_compare_dependent_correlations}
\alias{kk_compare_dependent_correlations}
\title{Compare Dependent Correlations}
\usage{
kk_compare_dependent_correlations(
  data,
  x = NULL,
  y = NULL,
  z = NULL,
  rxz = NULL,
  ryz = NULL,
  rxy = NULL,
  n = NULL,
  alternative = c("two.sided", "less", "greater")
)
}
\arguments{
\item{data}{A data frame or a single correlation coefficient ($r_{XZ}$). If a data frame is provided,
can specify raw variables (\code{x}, \code{y}, \code{z}) or pre-calculated fields (\code{rxz}, \code{ryz}, \code{rxy}, \code{n}).}

\item{x}{If \code{data} is a data frame and raw data is used, the column representing variable $X$.
If pre-calculated values are used, the correlation coefficient $r_{YZ}$.}

\item{y}{If \code{data} is a data frame and raw data is used, the column representing variable $Y$.
If pre-calculated values are used, the correlation coefficient $r_{XY}$.}

\item{z}{If \code{data} is a data frame and raw data is used, the column representing variable $Z$.
If pre-calculated values are used, the sample size $n$.}

\item{rxz}{If \code{data} is a data frame and pre-calculated statistics are used, the column containing $r_{XZ}$.}

\item{ryz}{If \code{data} is a data frame and pre-calculated statistics are used, the column containing $r_{YZ}$.}

\item{rxy}{If \code{data} is a data frame and pre-calculated statistics are used, the column containing $r_{XY}$.}

\item{n}{If \code{data} is a data frame and pre-calculated statistics are used, the column containing sample size $n$.}

\item{alternative}{Character string specifying the alternative hypothesis.
Must be one of \code{"two.sided"} (default), \code{"less"}, or \code{"greater"}.}
}
\value{
A tibble with the following columns:
\itemize{
\item \code{method}: The test method used.
\item \code{alternative}: The alternative hypothesis.
\item \code{statistic_name}: Name of the test statistic ("t").
\item \code{statistic}: Steiger's t-statistic.
\item \code{df}: The degrees of freedom ($n - 3$).
\item \code{p_value}: The p-value of the test.
\item \code{rxz}: Pearson correlation between $X$ and $Z$.
\item \code{ryz}: Pearson correlation between $Y$ and $Z$.
\item \code{rxy}: Pearson correlation between $X$ and $Y$.
\item \code{n}: The sample size.
}
}
\description{
Evaluates whether two variables ($X, Y$) correlate differently with a common third
criterion variable ($Z$) within the same sample, using Steiger's t-statistic.
}
\examples{
# Example 28e: Dependent correlations (sugar vs cavities vs salt)
kk_compare_dependent_correlations(rxz = 0.955, ryz = 0.52, rxy = 0.37, n = 5)

}
