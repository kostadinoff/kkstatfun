% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rdd.R
\name{kk_rdd}
\alias{kk_rdd}
\title{Regression Discontinuity (KK)}
\usage{
kk_rdd(
  data,
  outcome,
  running,
  cutoff = 0,
  treatment = NULL,
  bandwidth = NULL,
  kernel = c("triangular", "uniform", "epanechnikov"),
  degree = 1,
  covariates = NULL,
  vcov_type = "HC1",
  bins = 20,
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame.}

\item{outcome}{Outcome column (bare name or string).}

\item{running}{Running (forcing, assignment) variable column.}

\item{cutoff}{Threshold value of the running variable (default 0).}

\item{treatment}{Optional actual-treatment column for a fuzzy design.}

\item{bandwidth}{Bandwidth on the scale of the running variable. \code{NULL}
(default) selects the MSE-optimal bandwidth of Imbens & Kalyanaraman
(2012).}

\item{kernel}{Weighting kernel: \code{"triangular"} (default), \code{"uniform"} or
\code{"epanechnikov"}.}

\item{degree}{Polynomial order fitted on each side (default 1, local
linear). Orders above 2 are not recommended.}

\item{covariates}{Optional character vector of covariate names, entered
linearly. Covariates should not be affected by the treatment.}

\item{vcov_type}{Heteroscedasticity-robust variance type (default \code{"HC1"}).}

\item{bins}{Number of bins per side used for the plotting data (default 20).}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
One-row tibble with the RD \code{estimate}, \code{std.error}, confidence
interval, \code{p.value}, the \code{bandwidth} used and effective sample sizes on
each side. Fuzzy designs add \code{first_stage} (the jump in treatment
probability) and \code{reduced_form} (the jump in the outcome). Attributes:
\code{binned} (binned means for plotting), \code{bw_sensitivity} (the estimate at
0.5x to 2x the bandwidth), and the fitted model via \code{\link[=kk_model]{kk_model()}}.
}
\description{
Estimates the causal effect of a treatment assigned by a
threshold rule: units just above the cutoff on a running variable get the
treatment, units just below do not, and near the cutoff the two are
otherwise comparable. The estimate is the jump in the outcome at the
cutoff from a local polynomial regression fitted separately on each side
with kernel weights.

In a \strong{sharp} design treatment is a deterministic function of the running
variable. Leave \code{treatment} \code{NULL} for that case. In a \strong{fuzzy} design
crossing the cutoff only changes the \emph{probability} of treatment - pass the
actual treatment column and the estimand becomes the local average
treatment effect on compliers, obtained by two-stage least squares with
the cutoff indicator as the instrument.

The estimate is local: it identifies the effect \emph{at} the cutoff, for units
near it, and says nothing about units far away. Two things should be
checked before believing it - that units cannot precisely manipulate their
position around the cutoff (\code{\link[=kk_rd_density]{kk_rd_density()}}) and that the estimate does
not swing with the bandwidth (returned in \code{bw_sensitivity}).
}
\examples{
set.seed(4)
n <- 1000
x <- runif(n, -1, 1)
y <- 1 + 0.8 * x + 0.5 * (x >= 0) + rnorm(n, 0, 0.3)
d <- data.frame(x = x, y = y)

kk_rdd(d, y, x, cutoff = 0)

}
\references{
Imbens GW, Kalyanaraman K (2012) Optimal bandwidth choice for
the regression discontinuity estimator. \emph{Review of Economic Studies}
79:933-959. Lee DS, Lemieux T (2010) Regression discontinuity designs in
economics. \emph{Journal of Economic Literature} 48:281-355.
}
\seealso{
\code{\link[=kk_rd_density]{kk_rd_density()}}, \code{\link[=kk_rkd]{kk_rkd()}}, \code{\link[=kk_iv]{kk_iv()}}
}
