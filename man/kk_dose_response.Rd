% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dose_response.R
\name{kk_dose_response}
\alias{kk_dose_response}
\title{Non-Linear Dose-Response Modelling via Splines}
\usage{
kk_dose_response(
  data,
  exposure,
  outcome,
  confounders = NULL,
  family = c("binomial", "gaussian", "poisson"),
  ref_value = NULL,
  n_knots = 3,
  alpha = 0.05
)
}
\arguments{
\item{data}{A data frame or tibble containing patient-level data.}

\item{exposure}{Column name (quoted or unquoted) for the continuous exposure variable.}

\item{outcome}{Column name (quoted or unquoted) for the outcome variable.}

\item{confounders}{Character vector of column names for adjustment variables (optional).}

\item{family}{Model family: \code{"binomial"} (logistic/ORs), \code{"gaussian"} (linear/diffs), or \code{"poisson"} (rate ratios). Default \code{"binomial"}.}

\item{ref_value}{Reference exposure value for comparisons (default \code{NULL}, which uses the median exposure).}

\item{n_knots}{Number of spline knots (default \code{3}).}

\item{alpha}{Significance level for confidence intervals (default \code{0.05}).}
}
\value{
A tidy tibble of class \code{kk_dose_response} containing:
\describe{
\item{exposure}{Exposure values across a 100-point prediction grid}
\item{estimate}{Predicted OR, RR, or linear difference relative to \code{ref_value}}
\item{conf.low}{Lower bound of 95\% CI for predicted effect}
\item{conf.high}{Upper bound of 95\% CI for predicted effect}
\item{ref_value}{Reference exposure level used for scaling}
}
}
\description{
Fits non-linear dose-response curves for continuous exposures using restricted cubic
splines (natural splines), reporting predicted Odds Ratios, Risk Ratios, or linear effects
relative to a reference exposure level, along with tests for overall association and non-linearity.
}
\details{
Follows methods in Woodward (2014) \emph{Epidemiology: Study Design and Data Analysis}, 3rd Edition, Chapter 10.
Fits restricted cubic splines using \code{splines::ns()}. Deviations from a simple linear model are evaluated
using a Likelihood Ratio Test comparing the spline fit against a simple linear model.
}
\examples{
library(dplyr)
set.seed(42)
sim_df <- tibble(
  bmi = runif(300, 18, 38),
  disease = rbinom(300, 1, prob = plogis(-4 + 0.15 * (bmi - 25) + 0.01 * (bmi - 25)^2))
)
kk_dose_response(sim_df, bmi, disease, family = "binomial", ref_value = 22)
}
