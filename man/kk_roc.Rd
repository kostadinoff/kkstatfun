% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc.R
\name{kk_roc}
\alias{kk_roc}
\title{ROC / AUC with Optimal Cutoff (KK)}
\usage{
kk_roc(data, truth, predictor, conf.level = 0.95, direction = "auto")
}
\arguments{
\item{data}{Data frame.}

\item{truth}{Binary outcome column (bare name or string).}

\item{predictor}{Continuous marker / predicted-probability column
(bare name or string).}

\item{conf.level}{Confidence level for the AUC CI (default 0.95).}

\item{direction}{Comparison direction passed to \code{pROC::roc}
(default "auto").}
}
\value{
Tibble with the AUC and its CI, the Youden index, the optimal
threshold, and the sensitivity / specificity / PPV / NPV at that threshold.
}
\description{
Builds an ROC curve for a continuous marker against a binary
truth, returning the area under the curve (AUC) with a DeLong confidence
interval and the Youden-optimal cutoff together with the sensitivity and
specificity achieved there.
}
\examples{
\dontrun{
df <- data.frame(
  disease = rbinom(200, 1, 0.4),
  marker = rnorm(200)
)
kk_roc(df, disease, marker)
}

}
