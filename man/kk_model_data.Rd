% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{kk_model_data}
\alias{kk_model_data}
\title{Extract the Model-Fitting Data from a kkstatfun Result}
\usage{
kk_model_data(x)
}
\arguments{
\item{x}{A tibble returned by a kkstatfun modelling function.}
}
\value{
The data frame used for fitting, or \code{NULL} if none was recorded.
}
\description{
Returns the data frame the models in \code{x} were fitted to. Useful
as the \verb{data =} argument to \code{emmeans} in the rare case where automatic
recovery fails (typically after saving and reloading a model).
}
\examples{
set.seed(1)
d <- data.frame(y = rnorm(50), g = factor(rep(c("a", "b"), 25)))
fit <- kk_reg(d, y, "g")
nrow(kk_model_data(fit))

}
\seealso{
\code{\link[=kk_model]{kk_model()}}, \code{\link[=kk_emmeans]{kk_emmeans()}}
}
