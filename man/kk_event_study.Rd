% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did.R
\name{kk_event_study}
\alias{kk_event_study}
\title{Event-Study Difference-in-Differences (KK)}
\usage{
kk_event_study(
  data,
  outcome,
  unit,
  time,
  treat_time,
  covariates = NULL,
  reference = -1,
  leads = NULL,
  lags = NULL,
  cluster = NULL,
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame in long panel form, one row per unit-period.}

\item{outcome}{Outcome column (bare name or string).}

\item{unit}{Panel identifier column (bare name or string).}

\item{time}{Period column (bare name or string); numeric.}

\item{treat_time}{Column giving each unit's treatment adoption period
(bare name or string); \code{NA} or \code{Inf} marks never-treated units.}

\item{covariates}{Optional character vector of covariate column names.}

\item{reference}{Relative period used as the baseline (default \code{-1}).}

\item{leads}{Maximum number of pre-periods to estimate; earlier periods are
binned into the endpoint. \code{NULL} (default) estimates all of them.}

\item{lags}{Maximum number of post-periods to estimate; later periods are
binned into the endpoint. \code{NULL} (default) estimates all of them.}

\item{cluster}{Optional clustering column (defaults to \code{unit}).}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
Tibble with one row per relative period: \code{rel_time}, \code{period}
(\code{"pre"}, \code{"ref"} or \code{"post"}), \code{estimate}, \code{std.error}, confidence
interval, \code{statistic}, \code{p.value} and the number of treated observations
contributing. The joint Wald test of all leads is attached as the
attribute \code{pretrend}.
}
\description{
Estimates one treatment effect per period relative to
adoption, from a two-way fixed-effects regression of the outcome on a full
set of lead and lag indicators. The leads are the parallel-trends check:
if the groups were already diverging before the intervention, the
pre-period coefficients will differ from zero and the DiD estimate is not
credible. The lags trace out how the effect evolves after adoption.

One period - by default the last pre-treatment period, \code{reference = -1} -
is omitted as the baseline, so every coefficient is read relative to it.
Units that are never treated (\code{treat_time} of \code{NA} or \code{Inf}) contribute to
every period as pure controls.

Under staggered adoption these coefficients still borrow comparisons from
already-treated units; \code{\link[=kk_did_staggered]{kk_did_staggered()}} avoids that.
}
\examples{
set.seed(2)
units <- 60
d <- expand.grid(unit = seq_len(units), time = 1:8)
adopt <- ifelse(seq_len(units) <= 30, 5, Inf)
d$treat_time <- adopt[d$unit]
d$y <- 5 + 0.3 * d$time + rnorm(nrow(d), 0, 0.5) +
  2 * (d$time >= d$treat_time)

kk_event_study(d, y, unit, time, treat_time)

}
\references{
Sun L, Abraham S (2021) Estimating dynamic treatment effects in
event studies with heterogeneous treatment effects. \emph{Journal of
Econometrics} 225:175-199.
}
\seealso{
\code{\link[=kk_did]{kk_did()}}, \code{\link[=kk_did_staggered]{kk_did_staggered()}}
}
