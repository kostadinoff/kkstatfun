% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color_science.R
\name{kk_pal_check}
\alias{kk_pal_check}
\title{Audit a Palette for Figure Accessibility}
\usage{
kk_pal_check(
  colors,
  background = "#FFFFFF",
  min_dist = 0.1,
  type = .KK_VISION_TYPES,
  severity = 1,
  plot = FALSE
)
}
\arguments{
\item{colors}{Character vector of palette colours.}

\item{background}{Single background colour the figure is drawn on
(default \code{"#FFFFFF"}).}

\item{min_dist}{Minimum acceptable OKLab distance between two categories
(default 0.10). Values near 0.02 are at the just-noticeable-difference
level; 0.10 is a practical floor for categorical encoding in print.}

\item{type}{Vision types to audit (default: all five).}

\item{severity}{Severity of the deficiency, 0-1 (default 1, full dichromacy).}

\item{plot}{If TRUE, also draw the swatches via \code{\link[=kk_show_cvd]{kk_show_cvd()}}
(default FALSE).}
}
\value{
A tibble with one row per colour x vision type: \code{color}, \code{vision},
\code{simulated}, \code{contrast} (against \code{background}, as seen), \code{graphic} (TRUE
when contrast >= 3), \code{nearest} (the palette colour it is closest to),
\code{min_dist} and \code{distinct} (TRUE when \code{min_dist >= min_dist}).
}
\description{
Checks whether the categories in a palette stay distinguishable
for readers with colour vision deficiency, and whether each colour has
enough contrast against the plotting background. For every colour and every
vision type it reports the perceptual distance (Euclidean distance in
OKLab, "delta-E OK") to the \emph{nearest other colour in the palette} as that
reader would see it.

A pair separated by less than \code{min_dist} will read as the same colour: fix
it by changing the palette (see \code{\link[=kk_pal_safe]{kk_pal_safe()}}) or by adding a redundant
non-colour cue -- linetype, point shape or direct labels.
}
\details{
Summarise the audit with, for example,
\code{dplyr::filter(out, !distinct | !graphic)} -- an empty result means the
palette passes.
}
\examples{
# A red/green pair that collapses for deuteranopes
kk_pal_check(c("#D62828", "#2A9D2A"))

# The default flag palette
kk_pal_check(c("#D62828", "#003049", "#F77F00"))

}
\seealso{
\code{\link[=kk_pal_safe]{kk_pal_safe()}}, \code{\link[=kk_show_cvd]{kk_show_cvd()}}, \code{\link[=kk_contrast]{kk_contrast()}}.
}
