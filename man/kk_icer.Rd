% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health_economics.R
\name{kk_icer}
\alias{kk_icer}
\title{Cost-Effectiveness Analysis & Efficiency Frontier (KK)}
\usage{
kk_icer(data, cost, effect = NULL, strategy = NULL)
}
\arguments{
\item{data}{Data frame with one row per strategy, or a numeric vector of costs
(in which case \code{effect} and \code{strategy} are supplied separately).}

\item{cost}{Column of total costs per strategy (bare name or string), or, when
\code{data} is a numeric vector, a numeric vector of effects.}

\item{effect}{Column of total effects per strategy (bare name or string), e.g.
QALYs or life-years. Ignored when \code{data} is a numeric vector.}

\item{strategy}{Optional column of strategy labels (bare name or string). When
omitted, row order is used.}
}
\value{
Tibble ordered by cost with the incremental cost, incremental effect,
ICER, and a \code{status} column labelling each strategy as \code{"frontier"},
\code{"dominated"} (strongly dominated), or \code{"ext.dominated"} (extendedly
dominated). ICERs are computed only between adjacent non-dominated
strategies on the frontier.
}
\description{
Performs an incremental cost-effectiveness analysis across two or
more mutually exclusive strategies. Strategies are ranked by effectiveness,
strictly (strongly) dominated options are flagged, extendedly (weakly)
dominated options are removed by the standard iterative algorithm, and the
incremental cost-effectiveness ratios (ICERs) along the resulting efficiency
frontier are reported.
}
\examples{
ce <- data.frame(
  strategy = c("Standard care", "Drug A", "Drug B", "Drug C"),
  cost = c(2000, 8000, 12000, 30000),
  effect = c(3.5, 5.0, 5.5, 5.6)
)
kk_icer(ce, cost, effect, strategy)

}
