% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/butler_ks.R
\name{kk_butler_ks}
\alias{kk_butler_ks}
\title{Butler's Symmetry-Based Kolmogorov-Smirnov Test (KK)}
\usage{
kk_butler_ks(
  x,
  outcome = NULL,
  treatment = NULL,
  strata = NULL,
  n_sim = 9999,
  seed = NULL
)
}
\arguments{
\item{x}{A numeric vector of within-pair differences, or a data frame for the
stratified interface.}

\item{outcome, treatment, strata}{Column names (bare or string) for the
stratified interface. \code{treatment} must have exactly two levels; it is
coerced so the second level (or \code{1}/\code{TRUE}) is the treated group.}

\item{n_sim}{Number of Monte-Carlo randomizations (default 9999).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
One-row tibble with the observed statistic \code{D}, the Monte-Carlo
\code{p.value}, the \code{method}, the sample size, and \code{n_sim}. The full null
distribution is attached as the attribute \code{null_distribution}.
}
\description{
Fisherian randomization test of the sharp null hypothesis of no
treatment effect in matched-pair or stratified randomized experiments,
using Butler's symmetry-based Kolmogorov-Smirnov-type statistic. Unlike the
naive two-sample KS test it respects the pairing / stratification, and
unlike the Wilcoxon signed-rank test it retains power against highly
asymmetric or heavy-tailed effect distributions. The reference distribution
is built by sign-flipping (matched pairs) or within-stratum label
permutation, giving an exact Monte-Carlo p-value.

Two interfaces are provided:
\itemize{
\item \strong{Matched pairs} - pass \code{x} as a numeric vector of within-pair
differences (treated - control). The statistic tests symmetry about zero
and the null distribution is generated by random sign flips.
\item \strong{Stratified} - pass \code{x} as a data frame together with \code{outcome},
\code{treatment}, and \code{strata} columns. The weighted stratified statistic is
used and treatment labels are permuted within strata.
}
}
\examples{
# Matched-pair differences (treated - control)
set.seed(1)
diffs <- rnorm(40, mean = 0.6)
kk_butler_ks(diffs, n_sim = 999)

# Stratified randomized experiment
df <- data.frame(
  y = c(rnorm(50, 1), rnorm(50, 0), rnorm(40, 2), rnorm(40, 1)),
  trt = rep(c(1, 0, 1, 0), times = c(50, 50, 40, 40)),
  block = rep(c("A", "B"), times = c(100, 80))
)
kk_butler_ks(df, outcome = y, treatment = trt, strata = block, n_sim = 999)

}
