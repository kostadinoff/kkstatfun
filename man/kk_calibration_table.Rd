% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_validation.R
\name{kk_calibration_table}
\alias{kk_calibration_table}
\title{Calibration Table of a Risk Model (KK)}
\usage{
kk_calibration_table(data, truth, predicted, groups = 10)
}
\arguments{
\item{data}{Data frame.}

\item{truth}{Binary outcome column (bare name or string); coerced to 0/1.}

\item{predicted}{Predicted-probability column (bare name or string), on the
0-1 scale.}

\item{groups}{Number of risk groups for the calibration table and
Hosmer-Lemeshow test (default 10).}
}
\value{
Tibble (the per-group calibration table) with attributes \code{brier} and
\code{hosmer_lemeshow} (a one-row tibble with the chi-square statistic, degrees
of freedom, and p-value).
}
\description{
Assesses how well predicted probabilities from a binary risk
model agree with observed event rates. Returns the grouped calibration
table (mean predicted vs. observed risk within equal-count risk groups)
suitable for plotting a calibration curve, carrying the Brier score and the
Hosmer-Lemeshow goodness-of-fit test as attributes. This complements
discrimination measures such as \code{\link[=kk_roc]{kk_roc()}}.

For a one-row summary of calibration statistics (Hosmer-Lemeshow, O/E
ratio, calibration slope and intercept) rather than the per-group table,
use \code{\link[=kk_calibration]{kk_calibration()}}.
}
\examples{
set.seed(42)
df <- data.frame(y = rbinom(500, 1, 0.3))
df$p <- plogis(qlogis(0.3) + 0.5 * scale(df$y) + rnorm(500))
cal <- kk_calibration_table(df, y, p)
cal
attr(cal, "brier")
attr(cal, "hosmer_lemeshow")

}
\seealso{
\code{\link[=kk_calibration]{kk_calibration()}} for the one-row summary form.
}
