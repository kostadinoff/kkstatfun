% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/its.R
\name{kk_its}
\alias{kk_its}
\title{Interrupted Time Series (KK)}
\usage{
kk_its(
  data,
  outcome,
  time,
  intervention_time,
  offset = NULL,
  transition = 0,
  family = c("gaussian", "poisson", "quasipoisson"),
  harmonic = 0,
  period = NULL,
  nw_lag = NULL,
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame with one row per period, in any order.}

\item{outcome}{Outcome column (bare name or string): a count, rate or
continuous measure.}

\item{time}{Time column (bare name or string); numeric or \code{Date}. Periods
are assumed equally spaced.}

\item{intervention_time}{The first period \emph{after} the intervention, on the
scale of \code{time}.}

\item{offset}{Optional population/person-time column; entered as
\code{log(offset)} for count families, turning the model into one for rates.}

\item{transition}{Number of periods immediately after the intervention to
drop as a wash-out (default 0).}

\item{family}{\code{"gaussian"} (default), \code{"poisson"} or \code{"quasipoisson"}.}

\item{harmonic}{Number of Fourier pairs for seasonality (default 0). Needs
\code{period}.}

\item{period}{Seasonal period in observations (e.g. 12 for monthly data).}

\item{nw_lag}{Newey-West lag truncation. \code{NULL} (default) uses
\code{floor(4 * (n/100)^(2/9))}; \code{0} gives ordinary heteroscedasticity-robust
errors.}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
Tibble with one row per model term - \code{intercept}, \code{baseline_trend},
\code{level_change}, \code{trend_change} and any harmonics - with \code{estimate},
\code{std.error}, confidence interval and \code{p.value}; count families add the
exponentiated \code{ratio}. Attributes: \code{counterfactual} (per-period observed,
fitted and counterfactual values with an interval, ready to plot),
\code{impact} (the absolute and relative effect in the final period and the
cumulative difference over the whole post period) and \code{diagnostics}
(Durbin-Watson and Ljung-Box tests for residual autocorrelation).
}
\description{
Segmented regression for a single series observed before and
after a known intervention. Two effects are separated: the immediate
\emph{level change} at the moment of the intervention, and the \emph{trend change} -
the shift in slope that accumulates afterwards. A programme can look
successful on one and useless on the other, so both belong in the report.

The counterfactual is the pre-intervention trend extrapolated forward.
That is the design's weak point: it assumes nothing else changed at the
same time and that the pre-period trend would have continued. A
contemporaneous control series (\code{\link[=kk_synth]{kk_synth()}}) or a comparison group
(\code{\link[=kk_did]{kk_did()}}) tests that assumption; segmented regression alone cannot.

Serial correlation is the standard failure mode of ITS - ignoring it
makes the intervals far too narrow - so standard errors are
heteroscedasticity- and autocorrelation-consistent (Newey-West) by
default, and the residual autocorrelation diagnostics are returned.
}
\examples{
set.seed(9)
n <- 60
d <- data.frame(month = 1:n)
d$cases <- 100 - 0.5 * d$month - 15 * (d$month >= 37) -
  0.8 * pmax(0, d$month - 36) + rnorm(n, 0, 4)

res <- kk_its(d, cases, month, intervention_time = 37)
res
attr(res, "impact")

}
\references{
Bernal JL, Cummins S, Gasparrini A (2017) Interrupted time
series regression for the evaluation of public health interventions.
\emph{International Journal of Epidemiology} 46:348-355.
}
\seealso{
\code{\link[=kk_did]{kk_did()}}, \code{\link[=kk_synth]{kk_synth()}}, \code{\link[=kk_time_series]{kk_time_series()}}
}
