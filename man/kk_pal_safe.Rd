% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color_science.R
\name{kk_pal_safe}
\alias{kk_pal_safe}
\title{Build a Colour-Blind-Safe Qualitative Palette}
\usage{
kk_pal_safe(
  n,
  seed_colors = NULL,
  background = "#FFFFFF",
  lightness = c(0.45, 0.8),
  chroma = c(0.06, 0.2),
  min_contrast = 3,
  type = c("normal", "deutan", "protan", "tritan"),
  severity = 1
)
}
\arguments{
\item{n}{Number of colours required.}

\item{seed_colors}{Optional colours to keep at the start of the palette (for
example an institutional colour). They are used as-is and are not checked.}

\item{background}{Background colour the figure is drawn on
(default \code{"#FFFFFF"}).}

\item{lightness}{Length-2 range of OKLab lightness to sample from
(default \code{c(0.45, 0.80)}).}

\item{chroma}{Length-2 range of OKLCh chroma (default \code{c(0.06, 0.20)}).}

\item{min_contrast}{Minimum WCAG contrast against \code{background}
(default 3, the non-text threshold).}

\item{type}{Vision types the palette must satisfy simultaneously
(default deutan, protan, tritan plus normal vision).}

\item{severity}{Severity of the deficiency, 0-1 (default 1).}
}
\value{
Character vector of \code{n} hex colours, with the achieved minimum
pairwise OKLab distance in \code{attr(x, "min_dist")}.
}
\description{
Constructs \code{n} categorical colours that stay far apart in OKLab
space \emph{simultaneously} for normal, deuteranopic, protanopic and tritanopic
vision, and that clear the WCAG non-text contrast threshold against the
plotting background. Colours are chosen by deterministic farthest-point
sampling over a gamut-mapped OKLCh grid, so the same call always returns
the same palette -- important for reproducible figures.

Unlike picking from a fixed colour-blind-safe set, this lets you anchor the
palette on your own brand or institutional colours via \code{seed_colors} and
fill the remaining slots around them.
}
\details{
\code{"achroma"} is deliberately \strong{not} in the default \code{type}. Adding it
forces the categories to separate on lightness alone, which in practice
caps a usable palette at three or four colours -- and those then also have
to clear \code{min_contrast} against the background. If the figure must survive
photocopying, either ask for fewer categories with
\code{type = c("normal", "deutan", "protan", "tritan", "achroma")}, or keep a
larger colour palette and add a redundant non-colour cue (linetype, point
shape, direct labels). Check the result either way with \code{\link[=kk_pal_check]{kk_pal_check()}}.
}
\examples{
pal <- kk_pal_safe(5)
pal
kk_pal_check(pal)

# anchor on an institutional colour
kk_pal_safe(4, seed_colors = "#003049")

}
\seealso{
\code{\link[=kk_pal_check]{kk_pal_check()}}, \code{\link[=kk_show_cvd]{kk_show_cvd()}}, \code{\link[=set_plot_colors]{set_plot_colors()}}.
}
