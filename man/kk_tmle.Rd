% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmle.R
\name{kk_tmle}
\alias{kk_tmle}
\title{Targeted Maximum Likelihood Estimation of the ATE (KK)}
\usage{
kk_tmle(
  data,
  outcome,
  treatment,
  covariates,
  g_bounds = c(0.025, 0.975),
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame.}

\item{outcome}{Outcome column (bare name or string); binary (0/1) or
continuous.}

\item{treatment}{Binary treatment column (bare name or string); coerced so
its higher level / \code{1} / \code{TRUE} is "treated".}

\item{covariates}{Character vector of confounder column names.}

\item{g_bounds}{Truncation bounds for the estimated propensity score to
protect against positivity violations (default \code{c(0.025, 0.975)}).}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
One-row tibble with the targeted \code{ate}, its influence-curve
\code{std.error}, confidence interval, \code{p.value}, and the counterfactual mean
outcomes \code{ey1} and \code{ey0}. The naive (untargeted G-computation) estimate is
attached as the attribute \code{gcomp}.
}
\description{
Double-robust, locally efficient estimation of the average
treatment effect (ATE) - the risk difference for a binary outcome or the
mean difference for a continuous outcome - for a single-time-point binary
exposure. TMLE combines an outcome regression (the "Q" model) with the
treatment/propensity model (the "g" model) and performs a targeting
fluctuation step so that the estimator solves the efficient influence-curve
equation. The result is consistent if \emph{either} the outcome model or the
propensity model is correct (double robustness), with valid
influence-curve-based inference.

This implements the classic parametric TMLE with \code{glm} working models. For
ensemble/Super-Learner nuisance estimation or longitudinal/survival data,
use the dedicated \pkg{tmle} / \pkg{ltmle} packages; this function is the
transparent, dependency-free core for the point-treatment ATE.
}
\examples{
set.seed(1)
n <- 800
w1 <- rnorm(n); w2 <- rbinom(n, 1, 0.5)
a  <- rbinom(n, 1, plogis(-0.5 + 0.8 * w1 + 0.5 * w2))  # confounded
y  <- rbinom(n, 1, plogis(-1 + 0.6 * a + 1.0 * w1 + 0.5 * w2))
df <- data.frame(y, a, w1, w2)
kk_tmle(df, y, a, c("w1", "w2"))

}
