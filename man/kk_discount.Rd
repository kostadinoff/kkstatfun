% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health_economics.R
\name{kk_discount}
\alias{kk_discount}
\title{Discount a Stream of Costs or Effects (KK)}
\usage{
kk_discount(x, rate = 0.03, times = NULL)
}
\arguments{
\item{x}{Numeric vector of per-period values (costs or effects).}

\item{rate}{Annual discount rate (e.g. 0.03 for 3 percent).}

\item{times}{Optional numeric vector of times (in years) for each element of
\code{x}. Defaults to \code{seq_along(x) - 1}.}
}
\value{
One-row tibble with the undiscounted total, the discounted present
value, and the discount rate used.
}
\description{
Discounts a stream of future costs or health effects to present
value using the standard exponential discounting formula
\code{PV = sum(x_t / (1 + rate)^t)}. Times default to \verb{0, 1, ..., n-1} so the
first element is treated as occurring now (undiscounted).
}
\examples{
# A 2000-per-year cost stream over 5 years at 3.5 percent
kk_discount(rep(2000, 5), rate = 0.035)

}
