% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rdd.R
\name{kk_rd_density}
\alias{kk_rd_density}
\title{Manipulation Test for a Regression Discontinuity (KK)}
\usage{
kk_rd_density(
  data,
  running,
  cutoff = 0,
  bin_width = NULL,
  bandwidth = NULL,
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame.}

\item{running}{Running variable column (bare name or string).}

\item{cutoff}{Threshold value (default 0).}

\item{bin_width}{Histogram bin width. \code{NULL} (default) uses
\code{2 * sd(x) * n^(-1/2)}.}

\item{bandwidth}{Smoothing bandwidth for the local linear density fit.
\code{NULL} (default) uses \code{2 * sd(x) * n^(-1/5)}.}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
One-row tibble with the log density discontinuity \code{estimate}, its
\code{std.error}, confidence interval and \code{p.value}, plus the estimated
densities on each side. A non-significant result is what the design needs.
The binned density is attached as the attribute \code{density}.
}
\description{
Tests whether the density of the running variable jumps at the
cutoff. A discontinuity design assumes units cannot precisely control
which side of the threshold they land on; if they can - a clinician
nudging a score to secure treatment, a firm reporting just under a size
limit - the units on either side are no longer comparable and the design
fails. A pile-up on one side shows as a jump in the density.

This is the McCrary (2008) test: bin the running variable, then fit a
local linear regression of the binned density on each side of the cutoff
and test the log difference of the two intercepts.

Severe manipulation empties the bins just below the cutoff, which can push
the extrapolated intercept on that side below zero. Rather than take the
log of a negative number the function then falls back to the
kernel-weighted mean density on that side and warns; the fallback is
conservative, understating the jump it is reporting.
}
\examples{
set.seed(6)
x <- rnorm(2000)
kk_rd_density(data.frame(x = x), x, cutoff = 0)

}
\references{
McCrary J (2008) Manipulation of the running variable in the
regression discontinuity design: a density test. \emph{Journal of Econometrics}
142:698-714.
}
\seealso{
\code{\link[=kk_rdd]{kk_rdd()}}
}
