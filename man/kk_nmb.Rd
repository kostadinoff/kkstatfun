% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health_economics.R
\name{kk_nmb}
\alias{kk_nmb}
\title{Net Monetary & Net Health Benefit (KK)}
\usage{
kk_nmb(data, cost, effect = NULL, wtp = 50000, strategy = NULL)
}
\arguments{
\item{data}{Data frame with one row per strategy, or a numeric vector of costs.}

\item{cost}{Column of total costs (bare name or string), or a numeric vector of
effects when \code{data} is numeric.}

\item{effect}{Column of total effects (bare name or string). Ignored when
\code{data} is a numeric vector.}

\item{wtp}{Willingness-to-pay threshold(s); a scalar or numeric vector. The
result is expanded over all thresholds.}

\item{strategy}{Optional column of strategy labels (bare name or string).}
}
\value{
Tibble (strategies x thresholds) with \code{nmb}, \code{nhb}, and a logical
\code{optimal} marking the highest-NMB strategy at each threshold.
}
\description{
Computes the net monetary benefit (NMB) and net health benefit
(NHB) of one or more strategies at one or more willingness-to-pay (WTP)
thresholds, and flags the optimal (benefit-maximising) strategy at each
threshold. NMB = effect * WTP - cost; NHB = effect - cost / WTP.
}
\examples{
ce <- data.frame(
  strategy = c("Standard care", "Drug A", "Drug B"),
  cost = c(2000, 8000, 12000),
  effect = c(3.5, 5.0, 5.5)
)
kk_nmb(ce, cost, effect, wtp = c(20000, 50000), strategy = strategy)

}
