% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette.R
\name{kk_gen_palettes}
\alias{kk_gen_palettes}
\title{Generate Colour-Theory Palettes from Seed Colours}
\usage{
kk_gen_palettes(colors, n = 6, plot = FALSE)
}
\arguments{
\item{colors}{Character vector of 1–12 seed colours (hex or R colour names).}

\item{n}{Number of colours per palette (default 6).}

\item{plot}{If TRUE, also draw the swatches via \code{\link[=kk_show_palettes]{kk_show_palettes()}}
(default FALSE).}
}
\value{
A named list (class \code{kk_palettes}) of length-\code{n} hex vectors, with the
seed colours stored in \code{attr(x, "seed")}. Names: \code{custom} (multi-seed only),
\code{sequential}, \code{monochromatic}, \code{tints}, \code{shades}, \code{analogous},
\code{complementary}, \code{split_complementary}, \code{triadic}, \code{tetradic}, \code{spectral}.
}
\description{
From \strong{1 to 12} seed colours, derive a named catalogue of
palettes — sequential/monochromatic ramps and qualitative colour-theory
schemes (analogous, complementary, triadic, tetradic, …) — each returned as
exactly \code{n} hex colours ready for \code{\link[=kkplot]{kkplot()}} / \code{\link[=scale_fill_kk]{scale_fill_kk()}}. Schemes are
built in HSV space from the \strong{first} seed; the \code{sequential} ramp (and the
\code{custom} entry, present only when more than one seed is given) use all seeds
as anchors.

This is the programmatic counterpart to online generators such as
coolors.co: pick a scheme you like, then make it the plotting default with
\code{set_plot_colors(seed, scheme = "...")}.
}
\examples{
pals <- kk_gen_palettes("#D62828", n = 6)
pals$triadic
\dontrun{
kk_gen_palettes(c("#D62828", "#003049", "#F77F00"), n = 8, plot = TRUE)
}

}
\seealso{
\code{\link[=kk_show_palettes]{kk_show_palettes()}}, \code{\link[=set_plot_colors]{set_plot_colors()}}, \code{\link[=kk_pal]{kk_pal()}}.
}
