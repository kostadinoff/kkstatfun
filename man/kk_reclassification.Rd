% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reclassification.R
\name{kk_reclassification}
\alias{kk_reclassification}
\title{Net Reclassification Improvement (NRI) and Integrated Discrimination Improvement (IDI)}
\usage{
kk_reclassification(
  data,
  outcome,
  p_old,
  p_new,
  risk_thresholds = NULL,
  alpha = 0.05
)
}
\arguments{
\item{data}{A data frame or tibble containing patient-level data.}

\item{outcome}{Column name (quoted or unquoted) for binary patient outcome (1/0 or TRUE/FALSE).}

\item{p_old}{Column name (quoted or unquoted) for predicted risk probabilities from baseline model.}

\item{p_new}{Column name (quoted or unquoted) for predicted risk probabilities from new model.}

\item{risk_thresholds}{Optional numeric vector of risk cutoffs for categorical NRI (e.g. \code{c(0.10, 0.20)}).}

\item{alpha}{Significance level for confidence intervals (default \code{0.05}).}
}
\value{
An S3 object of class \code{kk_reclassification} containing:
\item{summary}{Tidy tibble with NRI_events, NRI_nonevents, NRI_overall, IDI, 95\% CIs, and p-values}
\item{reclass_events}{Reclassification cross-tabulation table for event cases}
\item{reclass_nonevents}{Reclassification cross-tabulation table for non-event controls}
}
\description{
Evaluates whether adding a new biomarker or risk factor improves patient risk predictions
over a baseline model using categorical/continuous NRI and IDI on patient-level data.
}
\details{
Implements methods described by Pencina et al. (2008, 2011) and Woodward (2014), Chapter 13.
Continuous $NRI_{>0}$ assesses any directionally correct change in predicted probability.
}
\examples{
library(dplyr)
set.seed(42)
patient_df <- tibble(
  outcome = rbinom(200, 1, 0.25),
  p_old = runif(200, 0.05, 0.40)
) \%>\%
  mutate(p_new = pmin(0.99, pmax(0.01, p_old + ifelse(outcome == 1, 0.10, -0.05))))

kk_reclassification(patient_df, outcome, p_old, p_new, risk_thresholds = c(0.15, 0.30))
}
