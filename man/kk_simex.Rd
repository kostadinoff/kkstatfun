% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simex.R
\name{kk_simex}
\alias{kk_simex}
\title{Simulation Extrapolation (SIMEX) for Measurement Error (KK)}
\usage{
kk_simex(
  model,
  variable,
  error_sd,
  lambda = c(0.5, 1, 1.5, 2),
  B = 100,
  degree = 2,
  seed = NULL
)
}
\arguments{
\item{model}{A fitted \code{lm} or \code{glm} object.}

\item{variable}{Name (bare or string) of the error-prone covariate.}

\item{error_sd}{Known/assumed standard deviation of the additive measurement
error (\code{sigma_u}).}

\item{lambda}{Grid of error-inflation factors (default \code{c(0.5, 1, 1.5, 2)}).
The naive fit (\code{lambda = 0}) is always prepended.}

\item{B}{Number of simulated datasets per \code{lambda} (default 100).}

\item{degree}{Degree of the polynomial extrapolant (2 = quadratic default,
1 = linear, 3 = cubic).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
Tibble with one row per model coefficient: the \code{naive} estimate, the
bias-corrected \code{simex} estimate, its SIMEX standard error (from the
variance-extrapolation of Carroll et al.), and a 95\% confidence interval.
Assumes the covariate enters the formula untransformed.
}
\description{
Corrects the bias that classical additive measurement error in a
continuous covariate induces in a fitted regression, using the SIMEX
algorithm of Cook and Stefanski. Increasing amounts of extra error are
simulated onto the mismeasured covariate, the model is refitted at each
error level, and the trend in the coefficients is extrapolated back to the
(hypothetical) zero-error condition. Works with any fitted \code{lm}/\code{glm}
(e.g. linear, logistic, Poisson).
}
\examples{
\dontrun{
set.seed(1)
u <- rnorm(500, 0, 1)          # true latent exposure
y <- 1 + 1.5 * u + rnorm(500)  # true relationship
x <- u + rnorm(500, 0, 0.8)    # observed, measured with error (sd 0.8)
naive <- lm(y ~ x)             # slope attenuated below 1.5
kk_simex(naive, x, error_sd = 0.8)
}

}
