% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apc.R
\name{kk_apc}
\alias{kk_apc}
\title{Age-Period-Cohort Analysis (KK)}
\usage{
kk_apc(data, age, period, count, pop)
}
\arguments{
\item{data}{Long data frame with one row per age-by-period cell.}

\item{age}{Age (group) column (bare name or string).}

\item{period}{Calendar period column (bare name or string).}

\item{count}{Event-count column (bare name or string).}

\item{pop}{Population / person-time column (bare name or string); enters as an
offset.}
}
\value{
A list with: \code{rates} (age x period observed rates), \code{net_drift} (annual
percent change with CI, estimable), and \code{models} (a deviance table for the
Age, Age-drift, Age-Cohort, Age-Period and Age-Period-Cohort Poisson models,
with likelihood-ratio tests for the period and cohort curvatures).
}
\description{
Analyses trends in rates by age, calendar period and birth cohort.
Because cohort = period - age, the three time scales are linearly dependent
and their individual linear trends are \strong{not separately identifiable} (the
identifiability problem). This function therefore reports only estimable
quantities: the age-specific rates, the overall \strong{net drift} (the common
annual rate of change, from an age-drift Poisson model), and a sequence of
nested Poisson models whose deviances isolate the \emph{non-linear} (curvature)
contributions of period and cohort, which \emph{are} identifiable.
}
\examples{
\dontrun{
d <- expand.grid(age = seq(30, 70, 10), period = seq(1980, 2010, 10))
d$pop <- 100000
d$count <- rpois(nrow(d),
  d$pop * exp(-9 + 0.05 * (d$age - 50) - 0.02 * (d$period - 1995)))
fit <- kk_apc(d, age, period, count, pop)
fit$net_drift
fit$models
}

}
