% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infectious_models.R
\name{kk_r0}
\alias{kk_r0}
\title{Basic Reproduction Number R0 (KK)}
\usage{
kk_r0(
  method = c("params", "growth", "final_size"),
  beta = NULL,
  gamma = NULL,
  r = NULL,
  sigma = NULL,
  attack_rate = NULL
)
}
\arguments{
\item{method}{One of \code{"params"}, \code{"growth"}, or \code{"final_size"}.}

\item{beta, gamma}{Transmission and recovery rates (method \code{"params"}).}

\item{r}{Exponential growth rate of the epidemic, per unit time (method
\code{"growth"}).}

\item{sigma}{Latent progression rate; supplying it uses the SEIR growth
relationship (method \code{"growth"}).}

\item{attack_rate}{Final proportion of the population infected, in (0, 1)
(method \code{"final_size"}).}
}
\value{
One-row tibble with the estimated \code{R0} and the method used.
}
\description{
Estimates the basic reproduction number \code{R0} by one of three
approaches: directly from transmission parameters, from the exponential
growth rate of an early epidemic, or from the final attack rate of a closed
epidemic. For the growth-rate method the SIR relationship \code{R0 = 1 + r / gamma}
is used, extended to \code{R0 = (1 + r / gamma)(1 + r / sigma)} when a latent rate
\code{sigma} is supplied (SEIR).
}
\examples{
kk_r0("params", beta = 0.5, gamma = 0.2)
kk_r0("growth", r = 0.15, gamma = 0.2)
kk_r0("final_size", attack_rate = 0.8)

}
