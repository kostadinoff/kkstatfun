% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/std_rates_ci.R
\name{kk_std_rates_ci}
\alias{kk_std_rates_ci}
\title{Directly Age-Standardized Rates with Exact Gamma Confidence Intervals}
\usage{
kk_std_rates_ci(
  data,
  count,
  pop,
  std_pop,
  multiplier = 1e+05,
  method = c("tiwari", "fay_feuer", "normal"),
  conf.level = 0.95
)
}
\arguments{
\item{data}{A data frame or tibble containing count, population, and standard population variables.}

\item{count}{Column name (quoted or unquoted) for observed counts/events in each stratum.}

\item{pop}{Column name (quoted or unquoted) for population size in each stratum.}

\item{std_pop}{Column name (quoted or unquoted) for standard population weights or counts.}

\item{multiplier}{Rate multiplier (default \code{100000} for rate per 100,000 person-years).}

\item{method}{CI estimation method: \code{"tiwari"} (default, Tiwari et al. 2006 modified gamma), \code{"fay_feuer"} (Fay & Feuer 1997 gamma), or \code{"normal"} (Wald normal approximation).}

\item{conf.level}{Confidence level for intervals (default \code{0.95}).}
}
\value{
A tidy tibble of class \code{kk_std_rates_ci} containing:
\describe{
\item{crude_rate}{Unadjusted crude rate per \code{multiplier}}
\item{std_rate}{Directly age-standardized rate per \code{multiplier}}
\item{se}{Standard error of standardized rate}
\item{conf.low}{Lower bound of standardized rate CI}
\item{conf.high}{Upper bound of standardized rate CI}
\item{multiplier}{Rate multiplier used}
\item{method}{CI calculation method applied}
\item{conf.level}{Confidence level}
}
}
\description{
Calculates directly age-standardized incidence or mortality rates with exact Gamma confidence intervals
using the Fay-Feuer (1997) or Tiwari et al. (2006) modified Gamma distribution methods (SEER standard).
}
\details{
Follows Szklo & Nieto (2014) \emph{Epidemiology: Beyond the Basics}, Chapter 3, and NCI SEER methodology
(Tiwari et al. 2006; Fay & Feuer 1997). Exact Gamma CIs provide valid coverage even when stratum event
counts are small or zero.
}
\examples{
library(dplyr)
std_df <- tibble(
  age_group = c("0-14", "15-64", "65+"),
  cases = c(2, 45, 120),
  pop = c(10000, 50000, 15000),
  std_pop = c(12000, 60000, 20000)
)
kk_std_rates_ci(std_df, cases, pop, std_pop, method = "tiwari")
}
