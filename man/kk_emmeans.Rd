% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emmeans.R
\name{kk_emmeans}
\alias{kk_emmeans}
\title{Tidy Estimated Marginal Means from a kkstatfun Model}
\usage{
kk_emmeans(
  x,
  specs,
  contrast = NULL,
  type = c("link", "response"),
  adjust = NULL,
  conf.level = 0.95,
  which = c("multivariable", "univariate"),
  predictor = NULL,
  group = NULL,
  ...
)
}
\arguments{
\item{x}{A tibble returned by \code{\link[=kk_reg]{kk_reg()}}, \code{\link[=kk_coxph]{kk_coxph()}}, \code{\link[=kk_rr_reg]{kk_rr_reg()}},
\code{\link[=kk_rate_reg]{kk_rate_reg()}} or \code{\link[=kk_firth]{kk_firth()}}.}

\item{specs}{A formula or character vector naming the predictors to average
over, passed straight to \code{\link[emmeans:emmeans]{emmeans::emmeans()}} (e.g. \code{~ arm}, \code{~ arm | sex}).}

\item{contrast}{Contrast method to apply, or \code{NULL} (default) for marginal
means only. Any method \code{\link[emmeans:contrast]{emmeans::contrast()}} accepts, e.g. \code{"pairwise"},
\code{"trt.vs.ctrl"}, \code{"poly"}.}

\item{type}{Passed to \code{emmeans}: \code{"link"} (default) keeps the model's link
scale, \code{"response"} back-transforms to odds ratios, rate ratios, or
probabilities as appropriate.}

\item{adjust}{Multiplicity adjustment for contrasts (default \code{"tukey"} for
pairwise; see \code{\link[emmeans:summary.emmGrid]{emmeans::summary.emmGrid()}}).}

\item{conf.level}{Confidence level (default \code{0.95}).}

\item{which}{Which stored model to use -- see \code{\link[=kk_model]{kk_model()}}.}

\item{predictor}{Predictor name when \code{which = "univariate"}.}

\item{group}{Group label when \code{x} came from grouped data -- see \code{\link[=kk_model]{kk_model()}}.}

\item{...}{Further arguments passed to \code{\link[emmeans:emmeans]{emmeans::emmeans()}}.}
}
\value{
A tibble of estimated marginal means, or of contrasts when
\code{contrast} is supplied. The underlying \code{emmGrid} is attached as
\code{attr(x, "emmGrid")} for follow-up work.
}
\description{
Runs \code{\link[emmeans:emmeans]{emmeans::emmeans()}} on the model kept inside a kkstatfun
modelling result and returns a \strong{tibble}, keeping the package's tidy-out
contract. Optionally computes pairwise (or other) contrasts in the same
call.

This is a thin convenience layer. For anything beyond marginal means and
contrasts -- \code{emtrends()}, \code{joint_tests()}, \code{emmip()}, custom contrast
matrices -- pull the model out with \code{\link[=kk_model]{kk_model()}} and call \code{emmeans}
directly.
}
\details{
Confidence-limit columns are normalised to \code{conf.low}/\code{conf.high}, because
\code{emmeans} names them \code{lower.CL}/\code{upper.CL} for models with finite degrees of
freedom but \code{asymp.LCL}/\code{asymp.UCL} for asymptotic ones such as a \code{glm} --
a difference that is easy to trip over when post-processing.
}
\examples{
set.seed(1)
d <- data.frame(
  age = rnorm(200, 60, 10),
  arm = factor(sample(c("Control", "Treatment"), 200, TRUE)),
  sex = factor(sample(c("F", "M"), 200, TRUE))
)
d$sbp <- 100 + 0.4 * d$age - 6 * (d$arm == "Treatment") + rnorm(200, 0, 8)

fit <- kk_reg(d, sbp, c("age", "arm"))

# Adjusted marginal means by arm
kk_emmeans(fit, ~ arm)

# Pairwise contrast between arms
kk_emmeans(fit, ~ arm, contrast = "pairwise")

}
\seealso{
\code{\link[=kk_model]{kk_model()}} to get the raw model, \code{\link[=kk_model_data]{kk_model_data()}} for its data.
}
