% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival_models.R
\name{kk_incidence_rate}
\alias{kk_incidence_rate}
\title{Incidence Rate and Rate Ratios with Exact CIs (KK)}
\usage{
kk_incidence_rate(
  data,
  cases,
  person_time,
  by = NULL,
  conf.level = 0.95,
  multiplier = 1000
)
}
\arguments{
\item{data}{Data frame (one or more rows; counts and person-time are summed
within each stratum).}

\item{cases}{Column of event counts (bare name or string).}

\item{person_time}{Column of person-time at risk (bare name or string).}

\item{by}{Optional stratifying column (bare name or string).}

\item{conf.level}{Confidence level (default 0.95).}

\item{multiplier}{Rate multiplier, e.g. 1000 for per-1000 person-years
(default 1000).}
}
\value{
Tibble with cases, person-time, incidence rate and exact CI per
stratum. If \code{by} is supplied, rate ratios (with CI and p-value) versus the
reference level are appended.
}
\description{
Computes incidence rates per unit of person-time with exact
(Poisson) confidence intervals. When a stratifying variable is supplied,
rate ratios versus the reference (first) level are also returned.
}
\examples{
df <- data.frame(
  arm = c("exposed", "unexposed"),
  cases = c(40, 15),
  pyears = c(1000, 1200)
)
kk_incidence_rate(df, cases, pyears, by = arm)

}
