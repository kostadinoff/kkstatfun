% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health_economics.R
\name{kk_partsa}
\alias{kk_partsa}
\title{Partitioned Survival Analysis (KK)}
\usage{
kk_partsa(
  pfs,
  os,
  times,
  state_costs,
  state_utilities,
  disc_cost = 0.03,
  disc_effect = 0.03
)
}
\arguments{
\item{pfs, os}{Progression-free and overall survival at each \code{times} point.
Either numeric vectors of survival probabilities, or \code{survfit} objects from
which the survival is read off at \code{times}. OS must be >= PFS at every point.}

\item{times}{Numeric vector of time points (in years) at which the curves are
evaluated; must start at 0.}

\item{state_costs}{Length-3 numeric vector of annual costs for the
Progression-Free, Progressed, and Dead states (named or in that order).}

\item{state_utilities}{Length-3 numeric vector of health utilities for the
three states (Dead is usually 0).}

\item{disc_cost, disc_effect}{Annual discount rates for costs and effects
(default 0.03 each).}
}
\value{
A list with a \code{trace} tibble (state membership over time) and a
one-row \code{summary} tibble of undiscounted and discounted life-years, QALYs and
costs.
}
\description{
Runs a three-state partitioned survival model (PartSA), the
standard cost-effectiveness framework in oncology HTA. Rather than modelling
transitions, it partitions the cohort at each time point directly from two
survival curves: progression-free survival (PFS) and overall survival (OS).
State membership is \code{PF(t) = PFS(t)}, \code{Progressed(t) = OS(t) - PFS(t)}, and
\code{Dead(t) = 1 - OS(t)}. Time in each state is integrated (trapezoidal rule,
which applies the half-cycle correction at the interval level) and combined
with per-unit-time state costs and utilities, discounted, to give total
life-years, QALYs and costs.
}
\examples{
t <- seq(0, 10, by = 0.1)
pfs <- exp(-0.5 * t)          # median PFS ~1.4y
os <- exp(-0.25 * t)          # median OS ~2.8y
kk_partsa(pfs, os, times = t,
          state_costs = c(PF = 12000, P = 8000, D = 0),
          state_utilities = c(PF = 0.8, P = 0.6, D = 0))

}
