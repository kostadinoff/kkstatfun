% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did.R
\name{kk_did}
\alias{kk_did}
\title{Difference-in-Differences (KK)}
\usage{
kk_did(
  data,
  outcome,
  treated,
  post,
  covariates = NULL,
  unit = NULL,
  time = NULL,
  cluster = NULL,
  family = c("gaussian", "binomial", "poisson"),
  offset = NULL,
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame.}

\item{outcome}{Outcome column (bare name or string).}

\item{treated}{Treatment-group indicator (bare name or string): 1/\code{TRUE}
for units ever exposed, 0 for the comparison group. Constant within unit.}

\item{post}{Period indicator (bare name or string): 1/\code{TRUE} for
observations after the intervention.}

\item{covariates}{Optional character vector of covariate column names,
entered linearly.}

\item{unit}{Optional panel identifier (bare name or string). When supplied,
unit fixed effects replace the \code{treated} main effect and, unless \code{cluster}
is given, standard errors are clustered on it.}

\item{time}{Optional period identifier (bare name or string). When supplied,
period fixed effects replace the \code{post} main effect.}

\item{cluster}{Optional clustering column for the standard errors
(defaults to \code{unit} when that is supplied, otherwise
heteroscedasticity-robust HC1).}

\item{family}{Outcome family: \code{"gaussian"} (default, effect on the outcome
scale), \code{"binomial"} or \code{"poisson"} (effect on the link scale, with the
exponentiated ratio-of-ratios also reported).}

\item{offset}{Optional offset column for \code{family = "poisson"} (person-time
or population); entered as \code{log(offset)}.}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
One-row tibble with the DiD \code{estimate}, \code{std.error}, confidence
interval, \code{statistic}, \code{p.value}, the number of clusters, and
\code{unadjusted} - the raw 2x2 difference-in-differences of the cell means.
For non-Gaussian families the exponentiated \code{ratio} and its interval are
appended. The 2x2 cell means are attached as the attribute \code{cells}, and
the fitted model is retrievable with \code{\link[=kk_model]{kk_model()}}.
}
\description{
Estimates the average treatment effect on the treated from a
before/after comparison of a treated and a control group. In the canonical
2x2 form the estimand is the interaction of \code{treated} and \code{post}: the
change in the treated group net of the change in the control group. With
\code{unit} and \code{time} supplied the same coefficient is estimated from a
two-way fixed-effects regression, which allows repeated observations and
more than two periods.

Identification rests on parallel trends: absent the intervention, the two
groups' outcomes would have moved together. That assumption is not
testable from a 2x2 design - use \code{\link[=kk_event_study]{kk_event_study()}} to inspect pre-period
leads before trusting the estimate.

With staggered adoption (units treated at different times) the two-way
fixed-effects coefficient is a variance-weighted average of 2x2
comparisons that can include already-treated units as controls, and is
biased when effects are heterogeneous over time. Use
\code{\link[=kk_did_staggered]{kk_did_staggered()}} in that case.
}
\examples{
set.seed(1)
n <- 400
d <- data.frame(
  unit = rep(seq_len(n), each = 2),
  post = rep(c(0, 1), times = n),
  treated = rep(rbinom(n, 1, 0.5), each = 2)
)
d$y <- 10 + 2 * d$post + 1.5 * d$treated -
  3 * d$treated * d$post + rnorm(nrow(d))

kk_did(d, y, treated, post)
kk_did(d, y, treated, post, unit = unit, time = post)

}
\references{
Angrist JD, Pischke JS (2009) \emph{Mostly Harmless Econometrics},
ch. 5. Princeton University Press.
}
\seealso{
\code{\link[=kk_event_study]{kk_event_study()}}, \code{\link[=kk_did_staggered]{kk_did_staggered()}}, \code{\link[=kk_its]{kk_its()}}, \code{\link[=kk_synth]{kk_synth()}}
}
