% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival_nnt.R
\name{kk_survival_nnt}
\alias{kk_survival_nnt}
\title{Time-Varying Number Needed to Treat (NNT) from Survival Data}
\usage{
kk_survival_nnt(data, time, status, group, times = NULL, conf.level = 0.95)
}
\arguments{
\item{data}{A data frame or tibble containing survival data.}

\item{time}{Column name (quoted or unquoted) for time-to-event.}

\item{status}{Column name (quoted or unquoted) for event status (1/0, TRUE/FALSE).}

\item{group}{Column name (quoted or unquoted) for binary treatment/exposure group.}

\item{times}{Numeric vector of evaluation times (default \code{NULL}, which evaluates at 25th, 50th, and 75th percentiles of event times).}

\item{conf.level}{Significance level for confidence intervals (default \code{0.95}).}
}
\value{
A tidy tibble of class \code{kk_survival_nnt} containing:
\describe{
\item{time}{Evaluation time point}
\item{S_control}{Kaplan-Meier survival probability in control group}
\item{S_treated}{Kaplan-Meier survival probability in treated group}
\item{ARR}{Absolute Risk Reduction}
\item{ARR_low}{Lower bound of ARR 95\% CI}
\item{ARR_high}{Upper bound of ARR 95\% CI}
\item{Metric}{"NNT" (if ARR > 0) or "NNH" (if ARR < 0)}
\item{NNT}{Estimated Number Needed to Treat/Harm}
\item{NNT_low}{Lower bound of NNT 95\% CI}
\item{NNT_high}{Upper bound of NNT 95\% CI}
\item{Note}{Significance or CI interpretation}
}
}
\description{
Computes time-varying Absolute Risk Reduction (ARR) and Number Needed to Treat (NNT) or Harm (NNH)
from Kaplan-Meier survival curves at specified time points, with Greenwood-based confidence intervals.
}
\details{
Follows Woodward (2014) \emph{Epidemiology: Study Design and Data Analysis}, 3rd Edition, Chapter 8.
Fits Kaplan-Meier curves for control and treatment groups via \code{survival::survfit}. Standard errors of
survival probabilities use Greenwood's formula, propagated to ARR and NNT.
}
\examples{
library(survival)
set.seed(42)
surv_df <- data.frame(
  time = rexp(200, rate = rep(c(0.05, 0.02), each = 100)),
  status = rbinom(200, 1, 0.8),
  rx = rep(c("Control", "Treated"), each = 100)
)
kk_survival_nnt(surv_df, time, status, rx, times = c(10, 20, 30))
}
