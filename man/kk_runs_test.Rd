% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/randomness.R
\name{kk_runs_test}
\alias{kk_runs_test}
\title{Runs Test for Randomness}
\usage{
kk_runs_test(
  data,
  x = NULL,
  method = c("single_sample", "up_down"),
  alternative = c("two.sided", "less", "greater"),
  threshold = NULL,
  tie_action = c("omit", "plus", "minus", "both"),
  continuity_correction = TRUE,
  conf.level = 0.95
)
}
\arguments{
\item{data}{A data frame or a vector. If a data frame is provided, the column
to analyze must be specified in the \code{x} argument.}

\item{x}{Column name (as symbol or string) to analyze if \code{data} is a data frame.
Ignored if \code{data} is a vector.}

\item{method}{Character string specifying the test method. Either \code{"single_sample"}
(default, Wald-Wolfowitz runs test for categorical data) or \code{"up_down"}
(Wallis-Moore runs test for serial randomness on numeric data).}

\item{alternative}{Character string specifying the alternative hypothesis.
Must be one of \code{"two.sided"} (default), \code{"less"} (too few runs), or \code{"greater"} (too many runs).}

\item{threshold}{Used only when \code{method = "single_sample"} and the input is numeric/continuous.
Can be a numeric value, \code{"median"}, \code{"mean"}, or \code{NULL} (default). If a value is provided,
continuous data is dichotomized. If \code{NULL}, the unique values are treated as distinct categories.}

\item{tie_action}{Used only when \code{method = "up_down"}. Action to take when consecutive values are equal (no change).
Must be one of \code{"omit"} (default, removes ties), \code{"plus"} (treats ties as increases),
\code{"minus"} (treats ties as decreases), or \code{"both"} (runs tests for both \code{"plus"} and \code{"minus"} and returns both).}

\item{continuity_correction}{Logical, whether to apply the correction for continuity
(subtracting 0.5 from the absolute difference between observed and expected runs) in the normal approximation.
Defaults to \code{TRUE}.}

\item{conf.level}{Confidence level for the interval (default: 0.95).}
}
\value{
A tibble with the following columns:
\itemize{
\item \code{method}: The test method used.
\item \code{alternative}: The alternative hypothesis.
\item \code{n_obs}: The effective number of observations.
\item \code{observed_runs}: The total number of runs ($r$).
\item \code{expected_runs}: The expected number of runs under the null hypothesis ($\mu$).
\item \code{variance_runs}: The variance of the number of runs ($\sigma^2$).
\item \code{z_statistic}: The standard normal $Z$-statistic.
\item \code{p_value}: The p-value of the test.
\item \code{tie_action}: (Only for \code{"up_down"}) The tie-handling action applied.
}
}
\description{
Performs runs tests to evaluate the randomness of a sequence.
Supports both the Single-Sample Runs Test (Wald-Wolfowitz test for nominal/categorical data)
and the Runs Test for Serial Randomness (up-down runs test for quantitative data).
The single-sample test is generalized to handle two or more categories.
}
\examples{
# Single-Sample Runs Test (Wald-Wolfowitz) - binary data
coin_tosses <- c("H", "H", "H", "T", "T", "T", "H", "H", "T", "T",
                 "H", "T", "H", "T", "H", "T", "T", "T", "H", "H")
kk_runs_test(coin_tosses)

# Multi-category Runs Test
die_rolls <- c("A", "A", "B", "C", "C", "B", "A", "A", "B", "B",
               "C", "C", "A", "C", "B", "A", "A", "B", "B", "B")
kk_runs_test(die_rolls, method = "single_sample")

# Up-down Runs Test for Serial Randomness
milk_dispensed <- c(1.90, 1.99, 2.00, 1.78, 1.77, 1.76, 1.98, 1.90, 1.65, 1.76,
                    2.01, 1.78, 1.99, 1.76, 1.94, 1.78, 1.67, 1.87, 1.91, 1.91, 1.89)
kk_runs_test(milk_dispensed, method = "up_down", tie_action = "plus")

# Tidy evaluation with a data frame
df <- data.frame(val = milk_dispensed)
kk_runs_test(df, val, method = "up_down")

}
