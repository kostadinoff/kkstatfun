% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_validation.R
\name{kk_iptw}
\alias{kk_iptw}
\title{Inverse-Probability-of-Treatment Weighting (KK)}
\usage{
kk_iptw(
  data,
  treatment,
  outcome,
  covariates,
  estimand = c("ATE", "ATT"),
  stabilize = TRUE,
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame.}

\item{treatment}{Binary treatment column (bare name or string); coerced to 0/1.}

\item{outcome}{Binary outcome column (bare name or string); coerced to 0/1.}

\item{covariates}{Character vector of covariate names for the propensity model.}

\item{estimand}{Target estimand: "ATE" (default) or "ATT".}

\item{stabilize}{Use stabilized weights (default TRUE).}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
A tibble with the weighted risk difference and risk ratio (with robust
confidence intervals), carrying the fitted weights and propensity scores as
the attributes \code{weights} and \code{propensity}.
}
\description{
Estimates a propensity score from the supplied covariates,
constructs inverse-probability-of-treatment weights (ATE or ATT, optionally
stabilized), and returns the weighted treatment effect on a binary outcome.
Pair with \code{kk_smd} to check covariate balance before and after weighting.
}
\examples{
\dontrun{
df <- data.frame(
  trt = rbinom(500, 1, 0.5),
  age = rnorm(500, 50, 10),
  sex = rbinom(500, 1, 0.5)
)
df$out <- rbinom(500, 1, plogis(-1 + 0.5 * df$trt + 0.02 * df$age))
kk_iptw(df, trt, out, covariates = c("age", "sex"))
}

}
