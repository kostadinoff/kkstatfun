% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv.R
\name{kk_ancova}
\alias{kk_ancova}
\title{Pre-Post ANCOVA (KK)}
\usage{
kk_ancova(
  data,
  outcome,
  treatment,
  baseline,
  covariates = NULL,
  vcov_type = "HC3",
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame, one row per participant.}

\item{outcome}{Follow-up outcome column (bare name or string).}

\item{treatment}{Treatment/group column; two levels.}

\item{baseline}{Baseline measurement of the same outcome.}

\item{covariates}{Optional character vector of further covariate names.}

\item{vcov_type}{Robust variance type (default \code{"HC3"}), or \code{NULL} for the
model-based variance.}

\item{conf.level}{Confidence level (default 0.95).}
}
\value{
One-row tibble with the ANCOVA-adjusted treatment \code{estimate}, its
\code{std.error}, confidence interval and \code{p.value}; the adjusted group means
\code{ey1} and \code{ey0} (standardised over the observed baseline distribution);
and the \code{change_score} and \code{posttest_only} estimates for comparison.
}
\description{
Estimates a treatment effect from a pretest-posttest design by
regressing the follow-up outcome on the treatment and the baseline value
of the same measure. ANCOVA is the efficient choice here: it beats both
the naive comparison of follow-up means (which ignores baseline imbalance)
and the comparison of change scores (which over-corrects whenever the
pre-post correlation is below one), and it is robust to regression to the
mean. All three estimates are returned so the difference is visible.

In a randomised trial baseline adjustment is a precision gain, not a bias
correction. In an observational pre-post comparison ANCOVA only removes
confounding by the baseline value itself - not by anything else.
}
\examples{
set.seed(8)
n <- 200
base <- rnorm(n, 140, 15)
arm <- rbinom(n, 1, 0.5)
post <- 0.6 * base + 56 - 8 * arm + rnorm(n, 0, 8)
d <- data.frame(base = base, arm = arm, post = post)

kk_ancova(d, post, arm, base)

}
\references{
Vickers AJ, Altman DG (2001) Analysing controlled trials with
baseline and follow up measurements. \emph{BMJ} 323:1123-1124.
}
\seealso{
\code{\link[=kk_did]{kk_did()}}, \code{\link[=kk_reg]{kk_reg()}}
}
