% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/median_test.R
\name{kk_median_test}
\alias{kk_median_test}
\title{Median Test for Independent Samples}
\usage{
kk_median_test(
  data = NULL,
  x = NULL,
  group = NULL,
  tie_action = c("below", "drop", "split"),
  alternative = c("two.sided", "less", "greater"),
  continuity_correction = TRUE,
  conf.level = 0.95
)
}
\arguments{
\item{data}{A data frame or a vector. If a data frame is provided, the column
to analyze must be specified in the \code{x} argument, and the grouping column in \code{group}.}

\item{x}{Column name (as symbol or string) to analyze if \code{data} is a data frame.
Ignored if \code{data} is a vector.}

\item{group}{Column name (as symbol or string) representing the independent groups if \code{data} is a data frame.
Must be provided as the second vector if \code{data} is a vector.}

\item{tie_action}{Character string specifying how to handle observations equal to the composite median.
Must be one of \code{"below"} (default, includes ties in the below/equal category),
\code{"drop"} (removes observations equal to the median), or
\code{"split"} (assigns half of the tie observations to above and half to below/equal).}

\item{alternative}{Character string specifying the alternative hypothesis for $k = 2$ tests.
Must be one of \code{"two.sided"} (default), \code{"less"}, or \code{"greater"}.}

\item{continuity_correction}{Logical, whether to apply continuity correction in the Chi-Square test
and Z-Test (default: \code{TRUE}).}

\item{conf.level}{Confidence level for the interval (default: 0.95).}
}
\value{
A tibble with the following columns:
\itemize{
\item \code{method}: The test method used.
\item \code{alternative}: The alternative hypothesis.
\item \code{statistic_name}: Name of the test statistic ("Chi-Square", "Fisher's p-value", or "Z").
\item \code{statistic}: The test statistic value.
\item \code{df}: The degrees of freedom for the Chi-Square test (or \code{NA} for others).
\item \code{p_value}: The p-value of the test.
\item \code{composite_median}: The calculated composite median value.
\item \code{table_summary}: A summary string of the counts above and below the median for each group.
}
}
\description{
Performs the Median Test for Independent Samples to evaluate if $k$ independent
groups differ in their central tendency. It compares the proportions of observations in
each group that fall above versus below/equal to the composite median of all groups combined.
When $k = 2$, it also returns Fisher's Exact Test and the proportions Z-Test.
}
\examples{
# Example 16.4: Standardized coordination scores (Males vs Females)
scores <- c(rep(10, 30), rep(2, 70), rep(10, 60), rep(2, 40))
gender <- c(rep("Males", 100), rep("Females", 100))
kk_median_test(x = scores, group = gender)

# Tidy evaluation with a data frame
df <- data.frame(scores = scores, gender = gender)
kk_median_test(df, scores, gender)

}
